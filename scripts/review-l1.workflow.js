
const diffPath = '/tmp/wave-l1-diff.patch'
const common = [
  'You are reviewing a shipped wave in the repo at /Users/hanxiao.du/Desktop/vincent/projects/pi-web (branch refactor/plugin-architecture).',
  'Read the diff first: ' + diffPath + ' (4 commits b5be3fcc..fe71b6b1: plugin panel style delivery via createRenderRoot adopt, drawer cascade coarse token, ContextSwitcherSheet heading removal, interactive-widget meta + keyboardInset docs, SHORT_VIEWPORT breakpoint published to pluginHostUi.breakpoints, PluginDialog presentation contract overlay|fullscreen, two-token touch density: 24 AA floor + 44 coarse floor with recorded exemptions).',
  'Live files: src/client/src/components/ChatView.ts, src/client/src/components/PromptEditor.ts, src/client/src/components/SessionList.ts, src/client/src/components/QuickSwitcher.ts, src/client/src/components/ExtensionDialogCard.ts, src/client/src/components/appShell/AppNavigationPanel.ts, src/client/src/components/appShell/AppContextSwitcher.ts, src/client/src/components/shared.ts, src/client/src/plugins/pluginHostUi.ts, src/client/src/plugins/types.ts, src/plugin-api.ts, src/client/src/components/PiWebApp.ts, pi-web-plugins/files/hostUi.ts, pi-web-plugins/terminal/hostUi.ts, scripts/probe-touch-targets.mjs, e2e/mobile.spec.ts.',
  'Output findings as a list; each finding must have file:line, a minimal failure scenario, and a true/false adjudication of your own suspicion (state which and why). End with a one-paragraph verdict. Write nothing to the repo.',
].join('\n')
const laneA = common + '\n\nLane focus: CONSISTENCY. Hunt for: (1) cascade-order bugs - any base rule after a pointer:coarse block that re-pins a property the coarse block raises (media queries add no specificity; the compact-status > button specificity win was one such bug, look for siblings); (2) contract drift between src/plugin-api.ts, src/client/src/plugins/types.ts, and test-fixtures/plugin-api-baseline/*.d.ts (PluginBreakpoints.shortViewport, PluginDialog.presentation must be identical); (3) the presentation contract wording: default overlay, must work at both presentations - check PiWebApp render + CSS actually satisfies must-survive-as-full-page (scrolling, sizing, z-index); (4) tests that assert the new behavior actually enumerate the states.'
const laneB = common + '\n\nLane focus: RED TEAM. Hunt for: (1) the 44px coarse bumps breaking desktop density or overflowing 393px rows (composer row overflow, session toolbar wrap, quick switcher chip row, extension dialog buttons in narrow containers); (2) the probe exemptions being too broad (HIT_SLOP_CLASSES substring matching could exempt unintended elements; msg-meta inline exemption hides a real tap target); (3) plugin dialogs rendered fullscreen losing close affordances or stealing the whole sheet; (4) adoptXHostStyles createRenderRoot pattern double-adopting or leaking sheets across plugin instances; (5) SHORT_VIEWPORT (max-height: 620px) now published - any host CSS keyed to it that plugins could now fight.'
const a = runs.run('lane-consistency', { agent: 'reviewer', task: laneA, model: 'botim-bllm/glm-5.3-flash:max' })
const b = runs.run('lane-redteam', { agent: 'reviewer', task: laneB, model: 'botim-bllm/qwen3.8-flash-next:max' })
const results = await Promise.all([a, b])
return { ok: true, laneA: results[0] && results[0].output, laneB: results[1] && results[1].output }
