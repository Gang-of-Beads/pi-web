const meta = { name: 'industry-lane-nav-shells', description: 'Relaunch the nav-shells research lane as one async background child with ambient web tools' };
var task = 'You are an autonomous industry-research lane for a frontend layout study. Use web_search (2-4 varied queries) and fetch_content / get_search_content to read design-system docs and articles, and fetch GitHub source (github.com file URLs or raw.githubusercontent.com) to verify how real products implement the patterns you report. Do not modify any repository file.\n\nOutput contract: numbered pattern cards, max 12, ordered by relevance. Each card = Pattern name / Problem it solves / How the industry does it (concrete, with numbers and behaviors) / Source(s) as URL or repo:path (mandatory; a card without a source is invalid) / Transferability guess to a dev-tool web app with a plugin architecture (one sentence) / Anti-pattern variant if any. Write findings in English. End with a one-paragraph lane verdict listing the 2-3 patterns you would bet on and why. If a source claim cannot be verified, drop the card rather than guessing.\n\nLANE THEME: navigation shells, drawers, sheets, panels, split views - how apps move between phone and desktop forms.\nHunt list: 1) Bottom tabs vs nav rail vs hamburger vs drill-in: documented tradeoffs (NN/g, Material, HIG) and which apps pick which. 2) Bottom sheet vs side drawer: scenario division and implementation libraries (vaul). 3) Master-detail / split view collapsing to single column on phones: documented patterns and state handling. 4) Panel collapse/expand affordances in tool-like apps (VS Code, Grafana, Sentry): how panel state persists and how fullscreen panel mode is entered on mobile. 5) Push vs overlay navigation on mobile web (does the drill push a route or float a sheet).\nSources to pull: shadcn/ui sheet docs + vaul repo readme, MUI Drawer docs, one VS Code/Grafana mobile behavior writeup, radix-ui primitives docs.';
var r = await runs.run('navshells', { agent: 'researcher', model: 'botim-bllm/glm-5.3-flash:max', task: task, worktree: false, async: true });
return { lane: 'nav-shells', result: typeof r === 'string' ? r.slice(0, 16000) : JSON.stringify(r).slice(0, 16000) };
