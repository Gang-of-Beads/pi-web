const meta = { name: 'layout-research-review', description: 'Three anonymous bllm review lanes over the mobile layout research report' };
var repo = '/Users/hanxiao.du/Desktop/vincent/projects/pi-web';
var doc = repo + '/docs/design/mobile-layout-research.md';
var common = 'You are an anonymous review lane for the PI WEB repo at ' + repo + '. Under review is a RESEARCH DOCUMENT (no code change): ' + doc + ' - a mobile-vs-desktop layout study of PI WEB with per-surface evidence, a plugin-seam map, and a triaged problem list (P0/P1/P2). Read the document first. Then verify its claims against the live sources (the paths it cites) and, where it references live measurements, against the measurement scripts (scripts/research-layouts.mjs, scripts/research-drawer-targets.mjs). Do not modify any file. This is an evidence audit, not a code review: your job is to find (a) claims the sources contradict, (b) evidence gaps (a finding with no file:line or measurement behind it), (c) triage mistakes (something labeled P1 that violates a standing rule, or P0 that is actually fine), (d) missed major findings visible in the cited screenshots ranges - do not guess at screenshots you cannot see; judge only what the document text and sources support.\n\nStanding rules the document must respect (from the repo AGENTS.md): every interactive target meets the coarse-pointer 44px floor (with a deliberate exemption policy only the owner can set); consistency is designed, not corrected; absence is not negation (empty means unloaded-or-empty); data carries the scope it belongs to.\n\nOutput contract: a numbered list of findings, max 10, ordered by severity. Each finding = document section + the exact claim you dispute + your evidence (file:line or reasoning) + your adjudication: TRUE (the document is wrong or missing something) or FALSE (the document holds; explain why). Mark disputed-but-unresolvable items as needs-owner. No praise padding. Findings in English. End with a one-paragraph overall verdict on whether the document is ready to hand to the owner as a decision basis.';
var lanes = [
  { key: 'glm-css-geometry', model: 'botim-bllm/glm-5.3-flash:max', task: common + '\nLANE FOCUS: the CSS/geometry claims. Verify every style-rule citation: src/client/src/components/ChatView.ts lines ~144-180 (drawer-tab min-height 22px at :152, the coarse media query at :164-168 setting drawer-collapse 44px, the base .drawer-collapse 32px re-declaration at :171, the comment at :159-163 about media-query placement); src/client/src/components/shared.ts:340-343 (action-row selected = accent); src/client/src/components/appShell/ContextSwitcherSheet.ts:91 (44x44 close button). Key dispute to settle: does the base 32px rule really defeat the coarse 44px rule given CSS cascade rules (same specificity, later origin wins; media queries add no specificity)? Check the actual rule order in the file and whether any other selector (drawer-control min-height 32px at :169-170) also plays in. Also check whether drawer-tab (the Goals chip) has ANY coarse-pointer override anywhere in the file; and whether the 44px floor exists as a design token (--pi-control-height-touch) that these rules ignore.' },
  { key: 'glm-seam-map', model: 'botim-bllm/glm-5.3-flash:max', task: common + '\nLANE FOCUS: the plugin-seam map (document section 3). Verify each consumer citation: PiWebApp.ts navSections bindings (:2149, :3788), the NavSectionContext surface field ("panel" | "sheet") and display flags (hidden/collapsible/collapsed/tiles/withCreate) - does a bare mode really not exist; ContextSwitcherSheet.ts:39-44 (host h2 Projects plus contributed section with withCreate:true - is the double heading real, and does the embedded ProjectList actually render its own Projects heading?); PromptEditor.ts:737/757 (composer slot filter and status lines); ChatView.ts:1659 findMessageRenderer; PiWebApp.ts:2611 visibleWorkspacePanels and the seven contributing plugins (grep pi-web-plugins/*/pi-web-plugin.ts for workspacePanels); WorkspacePanel.ts as the phone fullscreen host vs AppShell grid column 5. Findings wanted: seams the map got wrong or omitted (e.g. actions, workspaceLabels flow into navSections labelItems - PiWebApp.ts:2684), and any layout constraint the map asserts that the source does not support.' },
  { key: 'qwen-red-team', model: 'botim-bllm/qwen3.8-flash-next:max', task: common + '\nLANE FOCUS: adversarial - assume the document mis-triages something and the real P0 is hiding elsewhere. Attack the triage: 1) The P0.2 drawer-collapse cascade claim: if TRUE it means a recently shipped fix (commit f2c8dd25 claims "drawer-collapse 恢复 44px coarse 目标") is dead code - verify by reading the commit (git show f2c8dd25 -- src/client/src/components/ChatView.ts) whether the 32px base rule was already after the media query when the fix landed, or the fix itself put it there; if the fix shipped with the defect, the finding upgrades. 2) The files panel P0.1: check whether filesPanelElement.ts has adoptableStyles or injects host styles elsewhere (maybe the bare buttons are a theme-loading artifact, not a missing baseline); check what pi-web-plugins/files/hostUi.ts does. 3) The P2.8 composer crowding: count actual composer cluster targets at 393px (PromptEditor render) - is six real? 4) Missed surfaces: the document covers boot/sheet/sessions/chat/drawer/panels/settings/dock - what about terminal panel, quick switcher, machine dialog, ask-user card, session rename dialog? If one of those has a known 44px or layout violation visible in source, name it with file:line. 5) The document claims tap-target JSON lives in /tmp/layout-research - scratch data, fine - but check the committed scripts actually reproduce the numbers they claim (do the scripts write the JSON, or only print?).' },
];
var results = await runs.all(lanes.map(function (lane) {
  return { key: lane.key, agent: 'reviewer', model: lane.model, task: lane.task, worktree: false };
}));
return lanes.map(function (lane, i) {
  var r = results[i];
  return { lane: lane.key, findings: typeof r === 'string' ? r.slice(0, 14000) : JSON.stringify(r).slice(0, 14000) };
});
