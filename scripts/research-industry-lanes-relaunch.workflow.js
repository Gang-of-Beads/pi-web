const meta = { name: 'industry-lanes-relaunch', description: 'Relaunch the two failed research lanes as async background children with ambient web tools' };
var common = 'You are an autonomous industry-research lane for a frontend layout study. Use web_search (2-4 varied queries) and fetch_content / get_search_content to read design-system docs and articles, and fetch GitHub source (github.com file URLs or raw.githubusercontent.com) to verify how real products implement the patterns you report. Do not modify any repository file.\n\nOutput contract: numbered pattern cards, max 12, ordered by relevance. Each card = Pattern name / Problem it solves / How the industry does it (concrete, with numbers and behaviors) / Source(s) as URL or repo:path (mandatory; a card without a source is invalid) / Transferability guess to a dev-tool web app with a plugin architecture (one sentence) / Anti-pattern variant if any. Write findings in English. End with a one-paragraph lane verdict listing the 2-3 patterns you would bet on and why. If a source claim cannot be verified, drop the card rather than guessing.\n\n';
var lanes = [
  { key: 'nav-shells', model: 'botim-bllm/deepseek-v4-flash:max', task: common + 'LANE THEME: navigation shells, drawers, sheets, panels, split views - how apps move between phone and desktop forms.\nHunt list: 1) Bottom tabs vs nav rail vs hamburger vs drill-in: documented tradeoffs (NN/g, Material, HIG) and which apps pick which. 2) Bottom sheet vs side drawer: scenario division and implementation libraries (vaul). 3) Master-detail / split view collapsing to single column on phones: documented patterns and state handling. 4) Panel collapse/expand affordances in tool-like apps (VS Code, Grafana, Sentry): how panel state persists and how fullscreen panel mode is entered on mobile. 5) Push vs overlay navigation on mobile web (does the drill push a route or float a sheet).\nSources to pull: shadcn/ui sheet docs + vaul repo readme, MUI Drawer docs, one VS Code/Grafana mobile behavior writeup, radix-ui primitives docs.' },
  { key: 'anti-patterns', model: 'botim-bllm/glm-5.3-flash:max', task: common + 'LANE THEME (adversarial): documented failures and anti-patterns in mobile/desktop web UI - find the horror stories, not the success stories.\nHunt list: 1) NN/g mobile UX anti-patterns (hover-dependent menus, tiny targets, full-screen overlays for trivial content, infinite scroll traps). 2) The 100vh / keyboard overlap failure class with a concrete story. 3) position:fixed elements vs the mobile keyboard - real bug reports. 4) Drawer/sheet traps (cannot close, focus stuck, background scroll bleed) - issues in vaul or radix repos. 5) Input auto-zoom on iOS from font-size < 16px. 6) Double-tap zoom and touch delay legacies. 7) A responsive-redesign failure postmortem if findable.\nSources to pull: nngroup.com articles, web.dev, actual GitHub issues in vaul/radix-ui repos (link the issue), one postmortem. Each anti-pattern must name the concrete failure, not a generic worry.' },
];
var results = await runs.all(lanes.map(function (lane) {
  return runs.run(lane.key, { agent: 'researcher', model: lane.model, task: lane.task, worktree: false, async: true });
}));
return lanes.map(function (lane, i) {
  var r = results[i];
  return { lane: lane.key, cards: typeof r === 'string' ? r.slice(0, 16000) : JSON.stringify(r).slice(0, 16000) };
});
