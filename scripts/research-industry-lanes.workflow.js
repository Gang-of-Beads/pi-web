const meta = { name: 'industry-layout-research-lanes', description: 'Five adversarial researcher lanes surveying industry mobile/desktop layout and interaction practice' };
var common = 'You are an autonomous industry-research lane for a frontend layout study. Use web_search (2-4 varied queries) and fetch_content / get_search_content to read design-system docs and articles, and fetch GitHub source (fetch_content on github.com file URLs or raw.githubusercontent.com) to verify how real products implement the patterns you report. Do not modify any repository file. \n\nOutput contract: numbered pattern cards, max 12, ordered by relevance. Each card = Pattern name / Problem it solves / How the industry does it (concrete, with numbers and behaviors) / Source(s) as URL or repo:path (mandatory; a card without a source is invalid) / Transferability guess to a dev-tool web app with a plugin architecture (one sentence) / Anti-pattern variant if any. Write findings in English. End with a one-paragraph lane verdict listing the 2-3 patterns you would bet on and why. If a source claim cannot be verified, drop the card rather than guessing.\n\n';
var lanes = [
  { key: 'responsive-strategy', model: 'botim-bllm/glm-5.3-flash', task: common + 'LANE THEME: overall responsive strategy for web apps that must serve phones and desktops from one codebase.\nHunt list: 1) Material 3 adaptive design window size classes (compact/medium/expanded) - the actual mapping rules and thresholds. 2) Apple HIG guidance for adaptive layouts on iPadOS/web. 3) web.dev learn responsive design - current canonical advice. 4) Container queries: which production design systems ship them in 2024-2026 and for what (component-level vs page-level). 5) dvh/svh/lvh vs 100vh - the documented mobile keyboard/URL-bar failure and the fix adoption. 6) safe-area-inset handling in real apps. 7) How nav shells switch layout form between phone and desktop (nav rail -> bottom bar, side panel -> fullscreen) in at least one major design system.\nSources to pull: m3.material.io adaptive design pages, web.dev, primer/react layout components, shadcn/ui docs, one container-query adoption writeup.' },
  { key: 'touch-targets', model: 'botim-bllm/glm-5.3-flash', task: common + 'LANE THEME: touch-target floors and pointer interaction - platform mandates vs density reality.\nHunt list: 1) The exact numbers: WCAG 2.5.8 target size minimum (24px) vs AAA 2.5.5 (44px), Apple HIG 44pt, Material 48dp - and who uses which as a floor. 2) How dense professional tools (GitHub Primer, Shopify Polaris, Ant Design) define comfortable vs compact density and whether they document a minimum tap size. 3) Coarse-pointer media query overrides in production component libraries - real examples. 4) Hover-only affordances (hover menus, tooltips as the only label) - what the guidance says and how products degrade them on touch. 5) Secondary-action menus (the ... button on list rows) - documented hit-area practices.\nSources to pull: WCAG 2.5.8 text, HIG accessibility page, primer/react Button sizes source, polaris docs, antd size token source.' },
  { key: 'nav-shells', model: 'botim-bllm/deepseek-v4-flash', task: common + 'LANE THEME: navigation shells, drawers, sheets, panels, split views - how apps move between phone and desktop forms.\nHunt list: 1) Bottom tabs vs nav rail vs hamburger vs drill-in: documented tradeoffs (NN/g, Material, HIG) and which apps pick which. 2) Bottom sheet vs side drawer: scenario division and implementation libraries (vaul). 3) Master-detail / split view collapsing to single column on phones: documented patterns and state handling. 4) Panel collapse/expand affordances in tool-like apps (VS Code, Grafana, Sentry): how panel state persists and how fullscreen panel mode is entered on mobile. 5) Push vs overlay navigation on mobile web (does the drill push a route or float a sheet).\nSources to pull: shadcn/ui sheet docs + vaul repo readme, MUI Drawer docs, one VS Code/Grafana mobile behavior writeup, radix-ui primitives docs.' },
  { key: 'composer-input', model: 'botim-bllm/qwen3.8-flash-next', task: common + 'LANE THEME: chat composers and input surfaces on mobile web.\nHunt list: 1) visualViewport keyboard handling - the documented pattern (listen, offset, interactive-widget=resizes-content) and which products implement it. 2) Autogrow textarea: max-height and scroll strategy in chat UI libraries. 3) Send vs stop button state toggling in AI chat UIs - affordance details (icon swap, disabled logic, queue/steer semantics if any). 4) Attachment chips row above the composer - layout patterns. 5) Sticky toolbar vs inline toolbar under the keyboard. 6) Font-size <16px input zoom on iOS - the rule and workarounds.\nSources to pull: assistant-ui repo (composer components), one ai-sdk chat UI example, LibreChat web client source, a visualViewport deep-dive article.' },
  { key: 'anti-patterns', model: 'anthropic-merchant/claude-opus-5', task: common + 'LANE THEME (adversarial): documented failures and anti-patterns in mobile/desktop web UI - find the horror stories, not the success stories.\nHunt list: 1) NN/g mobile UX anti-patterns (hover-dependent menus, tiny targets, full-screen overlays for trivial content, infinite scroll traps). 2) The 100vh / keyboard overlap failure class with a concrete story. 3) position:fixed elements vs the mobile keyboard - real bug reports. 4) Drawer/sheet traps (cannot close, focus stuck, background scroll bleed) - issues in vaul or radix repos. 5) Input auto-zoom on iOS from font-size < 16px. 6) Double-tap zoom and touch delay legacies. 7) A responsive-redesign failure postmortem if findable.\nSources to pull: nngroup.com articles, web.dev, actual GitHub issues in vaul/radix-ui repos (link the issue), one postmortem. Each anti-pattern must name the concrete failure, not a generic worry.' },
];
var results = await runs.all(lanes.map(function (lane) {
  return { key: lane.key, agent: 'researcher', model: lane.model, task: lane.task, worktree: false };
}));
return lanes.map(function (lane, i) {
  var r = results[i];
  return { lane: lane.key, cards: typeof r === 'string' ? r.slice(0, 16000) : JSON.stringify(r).slice(0, 16000) };
});
