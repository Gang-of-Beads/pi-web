---
"@vincenthanxiaodu/pi-web": patch
---

Tell the reader a workspace has no sessions yet. The list rendered its heading and then blank space, so the only way forward was a control in the heading the eye had already passed.
