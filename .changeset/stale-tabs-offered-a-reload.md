---
"@gang-of-beads/pi-web": patch
---

A stale tab now knows it. When the server is upgraded, an open page keeps running the bundle it loaded and every fix shipped in between looks still broken; the page now compares versions when it becomes visible and offers a Reload banner - an offer, never an automatic reload.
