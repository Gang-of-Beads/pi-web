---
"@gang-of-beads/pi-web": patch
---

The bundled themes and terminal plugins load again, and dictation asks the right endpoint: a reserved id kept the theme pack from ever registering, browser plugin entries now bundle what they import, and voice calls its own daemon operation.
