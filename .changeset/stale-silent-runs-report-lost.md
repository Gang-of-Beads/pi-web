---
"pi-web": patch
---

Hours-old subagent runs that never wrote anything now report as Lost instead of Unknown when the parent is not streaming: the launch-grace silence rule applies to both branches. Unknown stays for the young window where nobody genuinely knows yet.
