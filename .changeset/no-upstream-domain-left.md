---
"@gang-of-beads/pi-web": patch
---

The last traces of the upstream domain are gone from living files. The sitemap that advertised the upstream site as this project's canonical identity is deleted, robots.txt no longer points at it, the dev-docs deploy config stops routing to a zone this repository does not own, canonical links point at the repository docs, and the screenshot tooling defaults to a local stack.
