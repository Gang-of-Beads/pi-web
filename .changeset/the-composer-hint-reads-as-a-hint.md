---
"@vincenthanxiaodu/pi-web": patch
---

Let the composer's trigger characters read as a hint. The empty field said "Message pi…  /  @  #", running the three affordance characters into the sentence where they looked like stray punctuation. The prompt now sits at the reading edge with the triggers grouped quietly at the trailing edge.
