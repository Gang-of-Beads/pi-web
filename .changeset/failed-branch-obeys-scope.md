---
"@gang-of-beads/pi-web": patch
---

The activity list shows running work first on every path: the retained rows drawn under a failed refresh now obey the same active scope as a healthy list, instead of flooding finished rows to the top exactly when loading hiccups.
