---
"@vincenthanxiaodu/pi-web": patch
---

The composer's history picker reaches the session's own prompts. The entrance read only this browser's localStorage, so a fresh device showed no door in front of a session holding fifteen thousand messages; the session's user messages are the same history arrived by another door. Measured on a clean profile: the button appears with zero local entries, and the picker lists what the server has.
