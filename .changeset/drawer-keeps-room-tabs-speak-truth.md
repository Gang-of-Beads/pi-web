---
"@gang-of-beads/pi-web": patch
---

The drawer keeps its room and every tab speaks its own truth: goals-absent no longer hides activity and notifications, failed plugins say so instead of posing as tidy empties, rename and pin from a browsed machine tab move there first, workspace rows land with their project so the session list stops waiting forever, filters reset when the tab changes, and a failed load can actually be retried.
