---
"@gang-of-beads/pi-web": patch
---

A message that speaks in images renders once. Supersession now compares words and images both, so a photo without a caption replaces its own echo instead of appearing twice, and two different photos sharing a caption are no longer merged into one message.
