---
"@vincenthanxiaodu/pi-web": patch
---

The waiting dialog's action row stays on screen: long details (task lists, contracts) cap and scroll inside the dialog card, and the waiting slot no longer scrolls the whole card — the confirm buttons no longer land below the fold of an inner scroller when a queue strip shares the screen.
