---
"@vincenthanxiaodu/pi-web": patch
---

Pinch zoom is off. Pinching moves the visual viewport under the layout viewport, which is the same signal a soft keyboard gives, so the shell shortened itself for a keyboard that was not there. Scale belongs to the app's own control in settings.
