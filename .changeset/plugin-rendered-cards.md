---
"@gang-of-beads/pi-web": patch
---

A custom message the app does not itself understand now renders as a card a plugin can draw, and as an honest "unrecognized message" card when no plugin claims it, instead of falling through to its raw model-facing text.
