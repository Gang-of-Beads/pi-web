---
"@vincenthanxiaodu/pi-web": patch
---

The Goals tab stays while its list loads, a failed listing keeps the goals it had instead of reading as "no goals", and the goal buttons say something when they cannot run: clicking Resume with no session selected now shows "Open a session in this workspace to run goal commands." instead of doing nothing. A command that does run refills the panel, so the card reflects the goal as it now is.
