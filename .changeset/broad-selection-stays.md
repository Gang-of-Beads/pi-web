---
"@gang-of-beads/pi-web": patch
---

Selecting a session that lives in a subdirectory of the chosen workspace no longer hands the Project chip to a deeper project while the session list keeps answering for the chosen one - an explicit broad selection that already contains the session stays selected.
