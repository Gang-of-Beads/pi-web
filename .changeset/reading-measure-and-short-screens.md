---
"@vincenthanxiaodu/pi-web": patch
---

Adapt the transcript to the screen it is on. On a wide display a message stretched the full width — about 150 characters a line at 2560px, where the eye loses its place returning to the next line — so the reading column is now bounded near 78 characters and centred. On a short one (a phone with the keyboard up, ~400px) the composer's input area is allowed to shrink so the transcript keeps more than a couple of visible lines.
