import "./TerminalPanel.js";

/**
 * Importing the panel module defines the element. The call exists so the
 * side effect is named at the call site rather than hiding in an import whose
 * only purpose a reader has to infer.
 */
export function defineTerminalPanel(): void {
  return undefined;
}
