import "./GoalPanel.js";

/**
 * Importing the panel module defines the element. The call names that side
 * effect at the point activation depends on it, rather than leaving a reader
 * to infer why the import is there.
 */
export function defineGoalPanel(): void {
  return undefined;
}
