/**
 * Re-exported so client code has one import for the rule while the definition
 * stays with the other session-state classification the server shares.
 */
export { isWaitingForUser } from "../../shared/sessionActivityState";
