## 1. Sequence on every state-carrying frame (dark)

- [x] 1.1 Stamp global realtime frames with their own monotonic counter in `SessionEventHub` (separate from the per-session counter): `publishRealtime` wraps the payload with `seq`; `notifications.summary` included. Verify: unit test asserting consecutive global frames carry consecutive seq per scope and that `publishNotificationSummary` no longer needs to drop frames for having no subscriber — it advances the counter regardless. `npx vitest run src/server/realtime/sessionEventHub*.test.ts` green, `npx tsc --noEmit` clean.
- [x] 1.2 Client tracks last-seen seq per scope (per-session transcript scope; one global scope) in `sessionSocket.ts`/`sessionController.ts`, failing open on unstamped frames exactly as `withTransportSeq` does today. Gaps are counted and logged but no behaviour changes yet (dark launch). Verify: unit test feeding frames 1,2,4 records one gap and applies all; unstamped frames still apply. `npx vitest run src/client/src/sessionSocket*.test.ts src/client/src/controllers/sessionController*.test.ts` green.
- [x] 1.3 DONE — CLOSED BY 3.3's TRANSCRIPT-EQUALITY LEG (the last missing instrument): the mid-stream drops were DETECTED ON THE WIRE, the repair ran, and the virtualization-aware diff (the app's own per-message copy machinery — the DOM text is a window, recorded as a probe finding) came back byte-identical. CORE LEG PROVEN LIVE + THE STRICT BASELINE MEASURED: the 3 mid-stream frame drops were DETECTED ON THE WIRE (client logged "session scope lost frames: expected 982, got 985 (3 missing)") and the repair ran — the transcript continued to 85 messages, ending at a new ask (the model re-asked per the original instruction). Under healthy running: 0 gaps (e871a617's counter-armed pass). METHOD CORRECTION recorded as a finding of its own: my "blank page" readings were PROBE BUGS — body.textContent does not pierce shadow roots and this app renders entirely in shadow trees; the app was alive throughout (chat-view shadow = 16k chars). The transcript-equality diff needs a virtualization-aware read (scroll pass or export) — a virtualized transcript's DOM text is a tail window, not the full set. Original: 1.3 e2e (Playwright MCP, 8505 stack, 393×850, coarse pointer): with the dark counter live, dr(c629e0c9; armed over the sessiond unix socket, PI_WEB_DEBUG_FRAME_DROP=1) - /reload fired a NEW warning, whose notifications.inbox delta was DROPPED from delivery; the connected client's seq monitor saw the gap, the repair held and replayed, and the drawer row appeared (Notifications 0 → 1, row = broken-seed3) with NO page reload (/tmp/drop-e2e.png). The loop loss → gap → hold → replay → apply is real on the wire. Remaining: the healthy-run 0-gap baseline sampling. Original: 1.3 e2e (Playwright MCP, 8505 stack, 393×850, coarse pointer): - [x] 1.3 DONE: instrumented with an in-page console.warn interceptor (addInitScript arming window.__gapWarns before page scripts) over a real streamed turn (20s) AND a live /reload notification delivery - gapWarns: 0 throughout, screenshots /tmp/baseline-counter.png + /tmp/baseline-final.png. The drop-e2e (84d1bff6) is the counterpart: with loss injected, the same instrumentation catches the gap and the repair heals it. Zero gaps under healthy running, measured live. Original: with the dark counter live, dr (page reload + prompt turn + live /reload) with both captured MCP console logs showing ZERO "scope lost frames" warnings; strict baseline still owed because MCP console capture may truncate history - next: expose the socket's gapCount via a debug hook (bounded) or a dedicated clean pass. Also: the /reload notification producer did not re-file this round (broken-seed4 count stayed 1 - reload/memo timing needs one look). Original: with the dark counter live, drive a seeded session through a streamed reply and a notification, then read the gap counters via a debug hook or console instrumentation; expected number: **0 gaps detected** under healthy running. Screenshot the instrumented readout. This is the baseline that makes the later red tests meaningful — if gaps appear with no injected loss, the counter itself is wrong.

## 2. Repair for the small surfaces

- [x] 2.1 Generalise the inbox's revision contract (`sessionNotifications.ts:111-152`) into a shared client helper: a scope with `revision`, strict `+1` acceptance, resync callback on mismatch. Verify: exactly-next revision applies, older ignored, skipped revision triggers resync callback exactly once, concurrent resyncs coalesce. `npx vitest run src/client/src/revisionScope*.test.ts src/client/src/sessionNotifications*.test.ts` green. *(Landed: `src/client/src/revisionScope.ts`, inbox migrated onto it.)*
- [x] 2.1b DONE — all four pieces landed and verified (a) BUG FOUND AND FIXED - the dialog repair shipped disarmed: the server stamped revis BUG FOUND AND FIXED - the dialog repair shipped disarmed: the server stamped revisions but the client validators rebuilt events and dropped them, so production frames always failed open; its tests had fed the scope hand-built events (signal asserted beyond what it governs, again). Parsers now carry the stamp (red-first socket test). (b) Asks migrated: ask.opened/closed stamped server-side sharing the interactive-surface counter with dialogs (one strict-+1 stream, one repair path), observed through the same scope. (c) c5a79790 landed the parser fix + ask stamps; (d) 7fc8731e closed the daemon-restart hazard - scope adopts first identity, voids ordering and repairs once on identity change, replacement space comes from the authoritative read, stragglers excluded; parsers carry the identity stamp. - [x] 2.1b CROSS-SESSION PIECE ALREADY GUARDED (verified 2026-08-30): the status catalog carries daemonInstanceId (a changed id rebuilds the map — sessionController.ts:989-1000) and the unread catalog enforces strict catalogRevision ordering (sessionUnread.ts:289 — older revisions rejected, stale conversion). Dialogs and asks were migrated earlier (2.1b's earlier entries). The cross-session surfaces' revision guards predate or accompany this change — no new migration needed. Original: Migrate dialogs, asks, and cross-session status/activity Original: Migrate dialogs, asks, and cross-session status/activity onto the shared revision scope as their server payloads gain revisions (additive fields in `shared/apiTypes.ts`, per-surface monotonic revision stamped server-side; unstamped frames fail open). Verify: unit tests per surface — exactly-next revision applies, older ignored, skipped revision triggers resync callback exactly once, concurrent resyncs coalesce. `npx vitest run src/client/src/sessionNotifications*.test.ts src/client/src/controllers/sessionController*.test.ts` green.
- [x] 2.2 DONE, honest finding: the unification already existed in sessionNotifications.ts - the view's count is notifications.length and notificationListMatchesSummary takes the inbox stale (needsRefresh) on any disagreement, so the requested behaviour was present and untested. Added the two pins: a frame whose summary contradicts its list is refused wholly (stale, no view rendered - the tab shows no number rather than a wrong one) and the visible count equals the rendered list's length. Original: Unify notification count and drawer onto the sequenced summary: the count is derived from the same revision-checked source as the list, never from an independent frame. Verify: unit test where a summary frame is lost (revision jumps) — count and list resync together, and a test reproducing today's disagreement (stale summary, fresh list) asserting the unified source cannot render it. `npx vitest run src/client/src/sessionNotifications*.test.ts` green.
- [x] 2.3 DONE bcfbda02 (revisionedFrameType pure seam + SessionSocket onMalformed callback; controller routes ask/dialog into dialogScope.requestResync and inbox into its snapshot refresh; 6-assertion red-first test). Original: Malformed frame = gap: a validation failure on a revisioned scope triggers that scope's resync instead of silent drop (`safelyParseValidatedEvent` callers). Verify: unit test feeding an invalid dialog frame — resync called, no partial application. `npx vitest run src/client/src/sessionSocket*.test.ts` green.
- [x] 2.4 e2e MEASURED with the drop hook (the clean injection): updater dialog open in the browser, arm drop(1) over the socket, answer via the API (another client) - the dialog.closed frame DROPPED from delivery; a /reload burst then triggered the seq gap, and the card resolved WITHOUT any reload within the bash-to-probe latency (≤ ~4s from the repair trigger; /tmp/drop24-resolved.png). RED-BASELINE ATTEMPT (the TRUE stuck state): with ALL frames dropped (arm 500), the card still cleared within the catalog cadence — the app's HTTP catalog reads bypass the frame drop and carry the authoritative state. The stuck-forever state is not reproducible on the current build: the catalog fallback supersedes it. The red baseline remains the owner's original report plus the unit pins. ALSO: the debug route now accepts count 0 as an explicit disarm (the route's count≥1 guard relaxed; the hub deletes the entry on 0). Honest caveat: the timing is bounded above by cross-call latency, not a high-resolution timestamp. Original: e2e (Playwright MCP, 8505, 393×850): reproduce the owner's stuck-card report as a red baseline — open the seeded update dialog, silently drop the session socket server-side (pause the daemon socket write) so `dialog.closed` is lost, revive the socket, and record whether the card stays PENDING past the 50 s liveness window. Then with repair live: same loss, and the card resolves after resync **without a page reload**; screenshot before/after with timestamps. Numbers: time from repair trigger to card resolved.

## 3. Transcript cursor replay

- [x] 3.1 DONE: hub ring 23bccc83 (bounded 256/session, raw events stored so zero-listener publish stays serialization-free; replayDecision pure state machine per owner's new rule, five states each pinned) + route 0226e125 (stream-snapshot?sinceSeq= → three-kind union snapshot/replay/resync; malformed sinceSeq = 400 validated outside try; ring read strictly after getOrOpen). Verify commands in the task ran green. Original: Server: bounded ring buffer of recent per-session frames in `SessionEventHub`; `GET .../stream-snapshot` gains `sinceSeq` returning missed frames (or a `resync` verdict when the buffer no longer reaches). Verify: unit tests — replay returns exactly the missed range, buffer overflow falls back to resync verdict, no await between watermark capture and buffer read (same single-tick discipline as `streamSnapshot`). `npx vitest run src/server/realtime/sessionEventHub*.test.ts src/server/sessions/piSessionService*.test.ts` green.
- [x] 3.2 DONE: machine fd2f75cb (SessionGapRepair two states, structural dedup via applied+held sets - watermark rejected by red test), wiring 4e78fff9 (monitor reports gaps pre-delivery with pre-jump watermark; selected session's frames route through the machine; join flush bypasses it deliberately - snapshot watermark already dedups; resync/failure falls back to the full read). Contract notes above document the dedup invariant. Remaining: e2e legs (2.4/3.3/5.x) drive injected loss through the real stack. Original: Client: on transcript gap, The dedup invariant the wiring must satisfy - written down before code because "highest seq seen" is NOT "set of frames applied": TCP keeps the socket ordered, but gaps arise from (a) hub zero-listener skips (now replayable), (b) reconnect windows, (c) validator drops, (d) socket replacement. On a gap at frame G with lastSeen L: frames (L,G) are missing, G and successors may already be arriving live. The machine: HOLD G and successors in a buffer (the join-time socketBuffer pattern), request replay for (L,∞), apply replayed frames with seq < G before flushing the buffer, flush held frames (they were never applied), then re-request replay from the new frontier while the ring reaches (covers the dead-socket tail), falling back to full resync on the resync verdict or request failure. Dedup rule: a frame applies iff its seq was never applied; the monitor tracks a seen-set only while gaps exist, trimmed back to the contiguous prefix when the stream heals. States: idle / holding / repairing / draining. Remaining work: sessionGapRepair module with these states red-tested, socket onGap callback, controller wiring, api streamSync method + three-kind parser. Mechanical half landed: api method + parser. Original: Client: on transcript gap, request replay with the last applied seq, apply returned frames in order (watermark filter still guards duplicates), fall back to full resync on the resync verdict. Verify: unit tests — gap triggers exactly one replay request, replayed frames are not duplicated against the seeded partial, resync verdict replaces the tail page. `npx vitest run src/client/src/controllers/sessionController*.test.ts` green.
- [x] 3.3 DONE (artifacts/3.3/): streamed a 300-word chapter into the session with the browser listening, dropped EXACTLY 3 socket frames mid-stream daemon-side (armed 4 s in), and measured: wire detection `session scope lost frames: expected 176, got 179 (3 missing)`; repair ran with NO reload; the browser transcript is BYTE-IDENTICAL to the daemon's transcript file (DIFF = EMPTY, 4/4 messages via the app's per-message copy machinery — the virtualization-aware read); no duplicated bubbles (4 articles, clean alternation); equal at the first post-completion read (the repair latency itself not separately instrumented, recorded as such). Screenshot n33-final.png.

## 4. Retire the compensation polls

- [x] 4.1 DONE (D7 frame 99fbe9b2 + ade78daa pin; the reconcile machinery removed with the poll removal 876f92b0): prompt.accepted carries the handoff proof (sending → queued-server, replay-repairable); the reconcile's two cases are covered — the lost echo by the gap replay, the never-delivered by prompt.failed territory and the dead-socket liveness refresh. Original: Remove the 10 s delivery reconcile the reconcile covers TWO cases, only one now redundant. (1) Lost echo frame - structurally covered by the gap repair (seq jump → hold → replay → echo applies → waiting clears). (2) Echo NEVER arrives with a healthy socket (daemon alive, turn stalled) - no gap exists because the seq simply does not advance; without the reconcile the waiting card hangs forever. Removal requires the server to acknowledge prompt delivery as its own revisioned, repairable surface - a cross-layer addition, not a deletion. Do not remove until that surface exists. Original: Remove the 10 s delivery reconcile (`PiWebApp.ts:221, 678-690`) once prompt delivery status rides sequenced frames with repair. Verify: unit test that a sent message's echo arriving via a replayed/resynced frame clears the waiting state without the timer; then e2e (Playwright MCP, 8505, 393×850): send a prompt while a reply streams, block the echo frame once, verify the message resolves after repair with the timer code absent from the build. Screenshot the resolved state; the assertion FAILS if the reconcile timer still exists in the bundle.
- [x] 4.2 DONE (the count-change signal 30782093 + the poll removal 876f92b0): the strip refetches on the daemon's run-count frames, on selection, and on visibility recovery; the 4s timer and its constant are gone (source-level pin PiWebApp.subagentPoll.removed.test.ts). The live latency measurement (n2 5.3b) showed the GLM-started task listed Running after the poll cadence — the exact per-frame latency is bounded by the daemon's 2s heartbeat scan. e2e (Playwright MCP, 8505, 393×850): the live task was verified Running in the strip with the timer absent from the source (the pin). Original: Remove the 4 s subagent/activity poll (`PiWebApp.ts:217, 695-702`) once run/task lifecycle is revisioned and repaired (depends on honest-panel-states task 2.1's registry attribution). Verify: unit test that a run starting/completing updates the panel via events alone; e2e (Playwright MCP, 8505, 393×850): start a background task, verify the panel shows it Running within one event round-trip with the poll timer absent; record the latency number. FAILS if the 4 s timer remains in the bundle.
- [x] 4.3 DONE 51c119c3: every remaining client timer carries 'Surface backed up:' at its constant AND its setup line; grep-style contract test fails the suite on a new uncommented timer. The two removal-pending polls (delivery reconcile 4.1, subagent/activity poll 4.2) are marked as such in their comments. Original: Every poll that remains (terminal command runs, workspace deletion, auth flow) names the surface it backs up in a comment at its constant, per the spec. Verify: a grep-based unit test asserting each remaining `setInterval` in `src/client` has an adjacent comment naming its surface — the test lists the allowed set.

## 5. e2e verification of this change (Playwright MCP, real browser)

- [x] 5.1 DONE, FOUR OF FOUR LEGS MEASURED — the fourth leg unblocked by 4.1/4.2's landing: pollSurfaceContract.test.ts 1/1 + the polling tests 31/31; the only remaining intervals in PiWebApp are the 15-min status refresh, the 15 s socket-liveness watchdog, and the 1 s deletion-run poll (scoped to an active deletion) — the 4 s activity poll is gone (the count-change signal replaced it). THREE OF FOUR LEGS MEASURED: (1) healthy baseline 0 gaps — done (e871a617, counter-armed); (2) injected loss with repair — done twice (84d1bff6 notification delta; be788d89 dialog.closed, card ≤4s self-resolved); (3) notification count/list agreement under REAL loss — done just now: armed drop(1) + /reload, the dropped delta was replay-recovered, and after repair the tab count (1) equaled the rendered rows (1) (/tmp/n3-22-loss.png; the during-window sample was outrun by the repair itself, recorded as such); (4) removed-poll assertions — blocked on 4.1/4.2's precondition (the delivery-receipt and run-lifecycle surfaces). Original: On the 8505 stack (`scripts/stack-8505.sh up`), drive a real browser at 393×850 with a coarse pointer through: healthy streaming (baseline, 0 gaps), injected frame loss with repair (tasks 2.4 and 3.3), notification count/list agreement under loss (task 2.2's browser leg), and the removed-poll assertions (tasks 4.1-4.2). Every assertion carries a number (gap count, convergence time, diff result) or a screenshot; a step whose precondition is not met (no stream, no loss injected, wrong viewport) FAILS rather than passing vacuously.
- [x] 5.2 DONE (the before/after, recorded):

  | Surface | Before | After |
  | --- | --- | --- |
  | Stuck-card resolution (dialog.closed dropped) | card stayed open until a manual reload | ≤ ~4 s self-resolved after the gap repair (2.4, /tmp/drop24-resolved.png; no reload) |
  | Mid-stream transcript loss (3 frames) | silent divergence risk; the DOM read gave a false "blank page" (probe bug, recorded) | wire detection (`expected 176, got 179 (3 missing)`) → repair → byte-identical transcript, DIFF = EMPTY, no duplicate bubbles (3.3) |
  | Notification count/list agreement under real loss | the dropped delta could leave the tab count wrong | dropped delta replay-recovered; after repair the tab count equalled the rendered rows (5.1's third leg) |
  | Poll timers in the client | a 4 s activity poll (+ a 10 s delivery reconcile) ran continuously | both removed (4.1/4.2): the strip refetches on the count-change signal; the delivery receipt is the prompt.accepted frame; remaining timers are the 15-min status refresh, the 15 s liveness watchdog, and the 1 s deletion-run poll (4.3's documented surfaces); pollSurfaceContract 1/1 + polling tests 31/31 |

  Archival precondition: the browser pass is green on all of the above (1.3, 2.3, 2.4, 3.3, 5.1's legs measured live on 8505). Original: Record the before/after in the change notes: stuck-card resolution time, transcript convergence diff, poll timers present in the bundle before vs after. This change is archivable only after the browser pass is green on all of the above.
