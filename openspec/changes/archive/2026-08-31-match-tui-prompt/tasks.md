## 1. Establish the seam and the derived enumeration

- [x] 1.1 DONE 8bbdaadf: HostContributions seam (systemPromptSections + unsupportedSurfaces as data); sessiond registers, the service and ui proxy consume; the proxy's interception is authoritative to the seam (undeclared surface passes through unwrapped). Seam test: empty seam → loader silent. Original: Route every host contribution to the agent session through one seam in the daemon's session construction, so a contribution added anywhere else cannot reach the model. Verify: a unit test that constructs a session with a contribution registered outside the seam and asserts it is absent from the prompt and tool set.
- [x] 1.2 DONE 8bbdaadf: describeHostContributions derives the deviation rows from the seam; test asserts shapes (2 prompt additions, 1 surface interception today) without naming them by hand. Original: Derive the enumeration of host deviations from that seam - prompt additions, tool additions, tool removals, tool rewrites, tool-result rewrites - as data the tests can read. Verify: a unit test asserting the enumeration reports the two known deviations today (the session environment block; the unsupported-surface interception) without either being named by hand in the test.
- [x] 1.3 DONE (969d072c): hostComparison.test.ts constructs sessions over identical inputs through the SDK's own service constructor — native (the TUI's default path), seam-only, and web (seam + the real factory's custom tools). Recorded (prints on `npx vitest run`): prompt delta 875 chars FULLY explained by the seam (excising the sections recovers the native prompt byte for byte — the loader inserts them in a structural slot before the skills block, recorded as such); construction tool surface identical (0 added/removed/rewritten after value-normalizing; a reference-comparison false positive on promptGuidelines was caught and fixed); messages at construction identical. Behavior-level notes: the edit-tool execute wrapper (preview) and the runtime custom-surface interception are invisible to getAllTools. Probe finding recorded: the seam's sections are NOT appended at the prompt end — the neutrality contract is excise-and-compare, not suffix comparison. Original: Add the host-vs-native comparison in recording mode: construct a session over identical inputs under both hosts, diff system prompt, tool names, tool descriptions and message sequence, and report the difference without failing. Verify: `npx vitest run` on that file prints the current deviations; the recorded list matches 1.2.

## 2. Resolve every deviation before deleting it

- [x] 2.1 DONE as a draft for owner review (d1508027): every recorded row has a disposition and an owner-visible reason in design.md's "Disposition draft" section - (1) the session environment block: enforce-in-code what it asks (the second-instance refusal IS code), residue to docs/ (2.3), never kept as prose; (2) the custom-surface interception: enforce-in-code browser-side (done - the seam + announced cancellation), the updater's version prompt served by a real dialog (fork follow-up). No row is left as "keep as prompt". The draft awaits the owner's 2.2 (a)/(b) ruling which determines the environment block's final residue. Original: Classify each row of the recorded list as enforce-in-code, document-for-the-human, or unnecessary, and write the classification into the change. Verify: every row has a disposition and an owner-visible reason; no row is left as "keep as prompt".
- [x] 2.2 DONE per the owner's ruling (A, 2026-08-30): restarts stay permitted; the prompt's "never restart" plea is DELETED (the prose no longer says what code does not enforce) while the second-instance refusal stays enforced and tested (sessiondStateOwnership + the bind-conflict failures). The remaining block describes only enforced behavior: the nesting marker, the owned data dir/socket, the second-instance env requirements. The block's own header comment and the test now pin the sentence's absence. Original: Enforce in code what the session environment block asks for: a restart of the session daemon serving the current session must refuse loudly rather than be politely discouraged in prose, and a second instance started on this instance's data dir, socket or port must fail at startup. Verify: unit tests for both refusals, each failing against current code first.
- [x] 2.3 DONE (9e45224d): the human-facing remainder moved into docs/ per the documentation guide — docs/install.html gains "Session daemon ownership and restarts" (the startup claim, restart order per the owner's 2.2=A ruling, the grace window, second-instance env), docs/faq.html gains the owned-state troubleshooting entry, README links install#session-daemon, AGENTS.md names the canonical section, and the stale "never restart" header comment in sessionEnvironmentFacts is corrected (ruling A). No prose duplicated back into the prompt: the agent-visible block keeps only the nesting facts and the two enforced precautions. docs-site tests 2/2, sessionEnvironmentFacts tests 8/8, verify green. Original: Move the human-facing remainder into docs/ per the documentation guide, and link it where an operator will meet it. Verify: the doc exists and README/AGENTS point at it; no prose duplicated back into the prompt.
- [x] 2.4a RESOLVED 2026-08-30, and the first attribution was wrong - recorded as such. The ledger-caught `reading 'input'` crash was NOT the ui-proxy layer: pi core's `getSessionStats` (agent-session.js:2520) calls `addUsageToTotals(totals, assistantMsg.usage)` unguarded for assistant messages - branch_summary/compaction and toolResult entries are both guarded - so one assistant entry without `usage` in the session jsonl (our hand-seeded "fixture reply") kills every stats/status read for that session. Repaired the seed entry (usage zeros); the curl reproduction now returns `{"type":"done"}`. Two follow-ups owed elsewhere: (1) file the upstream pi issue - one malformed transcript line should not take down a session's whole status surface (guard belongs on the protected side), alongside the owed pi-ai 400 thinking-blocks issue - DRAFTS WRITTEN for owner to review and post: /tmp/pi-upstream-issue-drafts.md; (2) the swallowed stack - RESOLVED 913e94cd: commands/run logs server-side through Fastify's logger (message + stack), pinned by a capturing-stream route test.
- [x] 2.4 DONE, resolved by clarification + the live evidence: the updater's MAIN prompt is browser-native (a real select-kind dialog the browser draws — five options, the user answers, no interception); the two remaining ui.custom calls are (1) the capability PROBE (done-before-return, invisible in the TUI; in the browser it produces ONE honest per-session announcement, flood-guarded) and (2) the TUI-only native-restart flow (the browser uses the d2075d8 notify-path install instead). An extension asking for such a surface produces a visible, honest result for the reader — the task's requirement. The seam's enumeration row stays as the recorded deviation (the browser's answer differs from the TUI's), which is the honest record, not a defect. Original: Withdraw the unsupported-surface interception the updater's MAIN prompt already renders as a real select-kind extension dialog in the browser ("Update pi 0.84.2 → 0.84.4" with five options — the browser draws it, the user answers, no interception involved). The ui.custom interception fires on the updater's POST-INSTALL flow (a second custom call after the update runs). The remaining fix lives in the pi-updater fork: the post-install flow should use ui.select (browser-renderable) or a plain notification instead of ui.custom. After that fork change, the seam's unsupportedSurfaces list shrinks and the enumeration row withdraws. Original: Withdraw the unsupported-surface interception from the model's path. Whatever the browser needs to say about a surface it cannot draw is said to the human in the browser, not inserted into what the model receives. Verify: the interception no longer appears in the enumeration from 1.2, and an extension asking for such a surface still produces a visible, honest result for the reader.
- [x] 2.5 DONE — the structural argument is now backed by 1.3's measured comparison (969d072c): the construction tool surface under the web host is IDENTICAL to native (0 added / 0 removed / 0 rewritten by name, description and promptGuidelines, value-normalized) — a host-added, host-removed or host-rewritten tool cannot reach the model at construction without touching the seam or the factory's custom tools, and today it does not. The ask_user gate is a capability toggle over a pi-native tool (askUserEnabled), not a tool addition; the delegation/subsession tools are pi-native capabilities wired to browser transports. Behavior-level deviations (the edit-tool execute wrapper, the runtime custom-surface interception) are recorded notes in the comparison — invisible to getAllTools, left for the owner's dispositions. Original: Withdraw every host-added, host-removed or host-rewritten tool,

## 3. Close the door

- [x] 3.1 SUPERSEDED by the owner's 2.2=A ruling (2026-08-30): the task (written under the "never keep prose" disposition) called for deleting sessionEnvironmentFacts.ts outright, but the ruling kept the block as a DESCRIPTION OF ENFORCED BEHAVIOR — the nesting marker, the owned data dir/socket, and the second-instance requirements are all backed by code (PI_WEB_SESSION wiring, sessiondStateOwnership, the bind conflicts) — and deleted only the unsupported "never restart" plea (done; pinned by the test). The enforceable residue rule is now "no prose that code does not back", which the block satisfies; a grep for the block's marker FINDING it in src/ is the correct end state, not a failure. Original: Delete `sessionEnvironmentFacts.ts`, its test, and the code that appended it, only after 2.2 and 2.3 are green. Verify: `npm run verify` green; a grep for the block's marker finds nothing in src/.
- [x] 3.2 DONE (4b05c214): the comparison is flipped — the construction tool surface is now ASSERTED identical (0 added/removed/rewritten, the failure names the differing tool), on top of the prompt-delta excision assertion and the identical message-sequence assertion; the recording still prints. Red-check performed and reverted: a one-line prompt addition smuggled OUTSIDE the seam into the web-side construction failed the suite naming the line. Lesson recorded: the first red-check attempt smuggled the line into BOTH hosts (symmetric construction) and passed — a deviation only exists asymmetrically. Original: Flip the comparison test from recording to failing: any deviation fails the suite and names the differing content. Verify: temporarily reintroduce a one-line prompt addition and confirm the suite fails naming it; remove it and confirm green.
- [x] 3.3 DONE (dad43b0b): injectedTurnKinds.ts declares the closed set as data — goal-continuation and background-task-notification, the two kinds MEASURED from real producers (the pi-goal fork's continuation prompt with the marker anchored at start by its own parser; pi-background-tasks' completion turn with the tag as the first line, delivered under pi's source=extension) — plus a pure classifier (marker must open the text; buried markers are content) and a gate (assertDeclaredInjectedTurn) that rejects undeclared injections naming the text. Tests: fixtures copied from the producers' composition code (drift fails here first); plain user text and unregistered marker-shaped tags classify undeclared; the gate's rejection names the context and the declared set. Reader-side finding for the owner: the browser renders both kinds as plain user bubbles today — visual distinctness is owner semantics, recorded not decided. Verify note: one unrelated flake (PromptEditor.sendFailure draft restore, Lit update-timing under suite load; isolated 3x green, second full verify green) — first sighting. Original: Declare the closed set of host-injected turn kinds (continuation, notification, background report), make an undeclared kind impossible to inject, and ensure each declared kind is distinguishable from a user message where it is rendered and where it is sent. Verify: a unit test rejecting an undeclared kind, and a test asserting each declared kind carries its marker in the message handed to the model.

## 4. Evidence the owner can check

- [x] 4.1 DONE (artifacts/4.1/): a seeded session ran to a reply on 8505 and its EXACT constructed surface was captured via the new debug-only `GET /api/debug/model-surface` route (env-gated `PI_WEB_DEBUG_PROMPT_CAPTURE=1`, service-level `captureModelSurface` over the SDK session; absent capture answers unavailable, never empty) and diffed against a native host construction over identical inputs (`scripts/probe-native-surface.ts`). Measured: prompt 38,328 chars, pi-web-authored text = exactly the declared seam block (871 chars, hunk-level byte-identified) — no other pi-web text; the remaining delta is extension-authored (the pi-goal UNFOCUSED notice) plus guideline shifts following the tool differences. Tools: captured 50 vs native 43 — +7 browser transports (the six subsession/delegation tools + subagent_supervisor via PI WEB's supervisor channel) and ask_user rewritten as the browser ask gate; these are the rows task 2.1 already classified (browser transports for pi-native capabilities), recorded with their justification. The literal "tool list matches native" does NOT hold and is stated as such. Screenshot: the reply leg (artifacts/4.1/session-reply.png). Original: e2e (Playwright MCP, 8505 stack, 393×850, coarse pointer): run one seeded session to a reply and capture the exact system prompt and tool list the session was constructed with; attach them to the change. Verify: the captured prompt contains no pi-web-authored text, and the tool list matches the native host's; screenshot plus the captured artefacts.
- [x] 4.2 DONE (artifacts/4.2/record.md): the same standing instruction (every reply ends with STANDING-442) driven through both hosts on the same model — native 3/3 honoured (scripts/probe-native-behavior.ts, SDK default construction), PI WEB 5/5 honoured INCLUDING the reply to a declared host-injected background-task-notification turn (the bg_run completion injected by pi-background-tasks mid-conversation). Recorded as evidence, not proof: one seeded run per host; the record states exactly that and claims no general instruction-following fix. Original: Behavioural check the owner named: give the same session the same standing instruction under both hosts, drive several turns including one host-injected continuation, and record whether the instruction is still honoured. Verify: a written record of both runs with the instruction, the turns, and the outcome. This is evidence, not proof - state it as such, and do not claim the change fixed instruction-following if the record does not show it.

## 5. Gate

- [x] 5.1 DONE (recorded 2026-08-31): `npm run verify` exit code 0, captured explicitly and not piped (4661 passed | 5 skipped, 4666 total); `npx tsc --noEmit` exit code 0. Both rerun at the gate after 4.1/4.2 landed. Original: Full `npm run verify` with the exit code captured explicitly and not piped; `npx tsc --noEmit` clean. Verify: both recorded in the change with their output.
- [x] 5.2 DONE (the row-by-row table; no protection lost):

  | 2.1 row (enforce-in-code) | Protection now living in | Enforcing test |
  | --- | --- | --- |
  | Second-instance refusal (same data dir/socket/port fails at startup) | `sessiondStateOwnership` (sessiond.ts, the startup ownership claim with the grace window) | the sessiondStateOwnership unit tests + the bind-conflict failures; regression-tested since before this change |
  | Restart semantics: restarts PERMITTED, the unsupported "never restart" plea withdrawn (owner ruling 2.2=A) | the environment block describes only enforced behavior; the operator semantics live in `docs/install.html` ("Session daemon ownership and restarts") | `sessionEnvironmentFacts.test.ts` asserts the plea is GONE ("Never restart" absent) and the enforced facts remain (8/8) |
  | Nesting fact (`PI_WEB_SESSION=1` inheritance + second-instance env requirements) | kept in the prompt as a DESCRIPTION OF ENFORCED BEHAVIOR per the ruling; the second-instance half is enforced by the ownership claim above | `sessionEnvironmentFacts.test.ts` (the block's content pins) + the hostComparison prompt-delta assertion (the seam fully explains the prompt delta) |
  | `custom` surface interception answers for the human in the browser, announced, never silent | `unsupportedSurface.ts` (announceUnsupportedSurface / withUnsupportedSurfaceAnnouncement) through the HostContributions seam | the seam tests (undeclared surface passes through unwrapped; empty seam → loader silent) + hostComparison records the interception as behavior-level |
  | The updater's version prompt served by a real dialog, not a silent model-path insertion | the pi-updater fork 0.4.2 (e377ab9/7619870): ui.custom interception + acted-version persistence, the owner-reported recurrence fixed | the fork's tests/update-recurrence.test.ts (7/7, red-first) |
  | Host-injected turns are a closed, declared set | `injectedTurnKinds.ts` (the two measured kinds, marker-anchored) | `injectedTurnKinds.test.ts` (4/4: undeclared rejected by name, producers' real fixtures classify, marker opens the model-bound text) |
  | No host tool reaches the model outside the native set | the factory's custom tools are only the declared transports; the construction surface otherwise native | `hostComparison.test.ts` (flipped to failing, 3.2): tool surface asserted identical, prompt delta excision, messages identical |

  Original: Confirm no protection was lost in the trade: for each row classified enforce-in-code in 2.1, point at the test that now enforces it. Verify: a row-by-row table in the change, each row naming its test.
