## 1. Sequence on every state-carrying frame (dark)

- [x] 1.1 Stamp global realtime frames with their own monotonic counter in `SessionEventHub` (separate from the per-session counter): `publishRealtime` wraps the payload with `seq`; `notifications.summary` included. Verify: unit test asserting consecutive global frames carry consecutive seq per scope and that `publishNotificationSummary` no longer needs to drop frames for having no subscriber — it advances the counter regardless. `npx vitest run src/server/realtime/sessionEventHub*.test.ts` green, `npx tsc --noEmit` clean.
- [x] 1.2 Client tracks last-seen seq per scope (per-session transcript scope; one global scope) in `sessionSocket.ts`/`sessionController.ts`, failing open on unstamped frames exactly as `withTransportSeq` does today. Gaps are counted and logged but no behaviour changes yet (dark launch). Verify: unit test feeding frames 1,2,4 records one gap and applies all; unstamped frames still apply. `npx vitest run src/client/src/sessionSocket*.test.ts src/client/src/controllers/sessionController*.test.ts` green.
- [ ] 1.3 e2e (Playwright MCP, 8505 stack, 393×850, coarse pointer): with the dark counter live, drive a seeded session through a streamed reply and a notification, then read the gap counters via a debug hook or console instrumentation; expected number: **0 gaps detected** under healthy running. Screenshot the instrumented readout. This is the baseline that makes the later red tests meaningful — if gaps appear with no injected loss, the counter itself is wrong.

## 2. Repair for the small surfaces

- [x] 2.1 Generalise the inbox's revision contract (`sessionNotifications.ts:111-152`) into a shared client helper: a scope with `revision`, strict `+1` acceptance, resync callback on mismatch. Verify: exactly-next revision applies, older ignored, skipped revision triggers resync callback exactly once, concurrent resyncs coalesce. `npx vitest run src/client/src/revisionScope*.test.ts src/client/src/sessionNotifications*.test.ts` green. *(Landed: `src/client/src/revisionScope.ts`, inbox migrated onto it.)*
- [ ] 2.1b Migrate dialogs, asks, and cross-session status/activity onto the shared revision scope as their server payloads gain revisions (additive fields in `shared/apiTypes.ts`, per-surface monotonic revision stamped server-side; unstamped frames fail open). Verify: unit tests per surface — exactly-next revision applies, older ignored, skipped revision triggers resync callback exactly once, concurrent resyncs coalesce. `npx vitest run src/client/src/sessionNotifications*.test.ts src/client/src/controllers/sessionController*.test.ts` green.
- [x] 2.2 DONE, honest finding: the unification already existed in sessionNotifications.ts - the view's count is notifications.length and notificationListMatchesSummary takes the inbox stale (needsRefresh) on any disagreement, so the requested behaviour was present and untested. Added the two pins: a frame whose summary contradicts its list is refused wholly (stale, no view rendered - the tab shows no number rather than a wrong one) and the visible count equals the rendered list's length. Original: Unify notification count and drawer onto the sequenced summary: the count is derived from the same revision-checked source as the list, never from an independent frame. Verify: unit test where a summary frame is lost (revision jumps) — count and list resync together, and a test reproducing today's disagreement (stale summary, fresh list) asserting the unified source cannot render it. `npx vitest run src/client/src/sessionNotifications*.test.ts` green.
- [x] 2.3 DONE bcfbda02 (revisionedFrameType pure seam + SessionSocket onMalformed callback; controller routes ask/dialog into dialogScope.requestResync and inbox into its snapshot refresh; 6-assertion red-first test). Original: Malformed frame = gap: a validation failure on a revisioned scope triggers that scope's resync instead of silent drop (`safelyParseValidatedEvent` callers). Verify: unit test feeding an invalid dialog frame — resync called, no partial application. `npx vitest run src/client/src/sessionSocket*.test.ts` green.
- [ ] 2.4 e2e (Playwright MCP, 8505, 393×850): reproduce the owner's stuck-card report as a red baseline — open the seeded update dialog, silently drop the session socket server-side (pause the daemon socket write) so `dialog.closed` is lost, revive the socket, and record whether the card stays PENDING past the 50 s liveness window. Then with repair live: same loss, and the card resolves after resync **without a page reload**; screenshot before/after with timestamps. Numbers: time from repair trigger to card resolved.

## 3. Transcript cursor replay

- [ ] 3.1 Server: bounded ring buffer of recent per-session frames in `SessionEventHub`; `GET .../stream-snapshot` gains `sinceSeq` returning missed frames (or a `resync` verdict when the buffer no longer reaches). Verify: unit tests — replay returns exactly the missed range, buffer overflow falls back to resync verdict, no await between watermark capture and buffer read (same single-tick discipline as `streamSnapshot`). `npx vitest run src/server/realtime/sessionEventHub*.test.ts src/server/sessions/piSessionService*.test.ts` green.
- [ ] 3.2 Client: on transcript gap, request replay with the last applied seq, apply returned frames in order (watermark filter still guards duplicates), fall back to full resync on the resync verdict. Verify: unit tests — gap triggers exactly one replay request, replayed frames are not duplicated against the seeded partial, resync verdict replaces the tail page. `npx vitest run src/client/src/controllers/sessionController*.test.ts` green.
- [ ] 3.3 e2e (Playwright MCP, 8505, 393×850): stream a long reply into the seeded session, drop exactly the socket frames for a bounded window mid-stream (daemon-side), release, and record: no page reload, transcript content equals the daemon's transcript file afterwards (diff = empty), no duplicated bubbles. Numbers: messages before/during/after, diff result, time to converge. Screenshot the converged tail.

## 4. Retire the compensation polls

- [ ] 4.1 Remove the 10 s delivery reconcile (`PiWebApp.ts:221, 678-690`) once prompt delivery status rides sequenced frames with repair. Verify: unit test that a sent message's echo arriving via a replayed/resynced frame clears the waiting state without the timer; then e2e (Playwright MCP, 8505, 393×850): send a prompt while a reply streams, block the echo frame once, verify the message resolves after repair with the timer code absent from the build. Screenshot the resolved state; the assertion FAILS if the reconcile timer still exists in the bundle.
- [ ] 4.2 Remove the 4 s subagent/activity poll (`PiWebApp.ts:217, 695-702`) once run/task lifecycle is revisioned and repaired (depends on honest-panel-states task 2.1's registry attribution). Verify: unit test that a run starting/completing updates the panel via events alone; e2e (Playwright MCP, 8505, 393×850): start a background task, verify the panel shows it Running within one event round-trip with the poll timer absent; record the latency number. FAILS if the 4 s timer remains in the bundle.
- [ ] 4.3 Every poll that remains (terminal command runs, workspace deletion, auth flow) names the surface it backs up in a comment at its constant, per the spec. Verify: a grep-based unit test asserting each remaining `setInterval` in `src/client` has an adjacent comment naming its surface — the test lists the allowed set.

## 5. e2e verification of this change (Playwright MCP, real browser)

- [ ] 5.1 On the 8505 stack (`scripts/stack-8505.sh up`), drive a real browser at 393×850 with a coarse pointer through: healthy streaming (baseline, 0 gaps), injected frame loss with repair (tasks 2.4 and 3.3), notification count/list agreement under loss (task 2.2's browser leg), and the removed-poll assertions (tasks 4.1-4.2). Every assertion carries a number (gap count, convergence time, diff result) or a screenshot; a step whose precondition is not met (no stream, no loss injected, wrong viewport) FAILS rather than passing vacuously.
- [ ] 5.2 Record the before/after in the change notes: stuck-card resolution time, transcript convergence diff, poll timers present in the bundle before vs after. This change is archivable only after the browser pass is green on all of the above.
