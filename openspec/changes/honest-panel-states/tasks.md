## 1. Goals panel: the four-way load slot

- [x] 1.1 Introduce the `PanelLoad<T>` slot type and migrate the goals state to it: replace `workspaceGoals`/`workspaceGoalsKey`/`workspaceGoalsLoading`/`workspaceGoalsFailed` with one keyed slot in `appState.ts`; `goalsForSelectedWorkspace` becomes a slot reader that returns the slot itself, not a collapsed `[]`. Verify: `npm run typecheck` clean; a unit test in `appState` asserting that a slot whose key differs from the selection reads `state: "unloaded"` for the new selection - never `loaded` with someone else's rows. (Done in `33f1e401`: the appState migration and the key-mismatch unit test; its test fixtures were completed in `5038926e`.)
- [x] 1.2 Wire the slot through `PiWebApp` (line 3128) into `ChatView` and `GoalPanel`; the GoalPanel renders Loading while `state === "loading"`, a failure line while `"failed"` (keeping matching rows per D5), and the empty claim only when `"loaded"` with a matching key and zero records. This kills the dead `.goalsLoading` wire. Verify: a GoalPanel unit test rendering all four states and asserting "No goals recorded for this workspace" appears only in the loaded-and-matching-and-empty case; `npx vitest run src/client/src/components/GoalPanel*.test.ts` green. (Done in `5038926e`: the slot travels whole from `PiWebApp` through `AppNavigationPanel` to `GoalPanel`; the panel mounts with a keyed slot in its tests; the intermediate split at the AppNavigationPanel call site, including the dead `.goalsKnown` wire, is gone.)
- [ ] 1.3 Live evidence on 8505 at 393×850, coarse pointer: with the seeded active goal in the project, open the goals panel and record (a) the loading line during a throttled read, (b) the goal card once loaded, (c) after switching to the seeded no-goal workspace, the empty claim only after the read completes. Screenshots for all three; the panel text during flight must not be an empty claim.

## 2. Background-task attribution from the registry

- [x] 2.1 Attribute tasks by ownership that survives compaction. As written this task said to read the session out of the registry directory name; that premise is false in this repo — the directory is named after the *server* pid because `BackgroundTaskContext.sessionId` is never supplied, so it carries no session identity (documented in the module header and the test fixture, and visible on disk as `session-<pid>-<pid>`). Implemented instead as recorded attribution: the first transcript sighting records taskId → transcript file name in a durable per-workspace file beside the registry, later lists attribute from the record, the transcript scan stays as collector and fallback, and first-writer-wins stops a quoting session from claiming another's task. Red test first (`ce08df59`), fix (`3f200e95`): `npx vitest run src/server/sessions/backgroundTasks.test.ts src/server/sessions/backgroundRunCount.test.ts` 20 passed, `npm run typecheck` 0 errors, lint clean.
- [ ] 2.1b An unreadable individual task record renders as status `unknown`, not absent (split out of 2.1, not started: `readTaskRecords` still skips unparseable records silently).
- [ ] 2.2 Live evidence on 8505 at 393×850: start a background task from the chat, confirm the activity strip shows it Running; then force the scenario of task 2.1 against the seeded session (compacted transcript with a live registry record) and screenshot the panel still listing it with "Nothing running right now." absent.

## 3. Goal reads cover every root a goal may live in

- [ ] 3.1 Server: when the focused session's cwd differs from `workspace.path`, read `.pi/goals/` under both; each record carries `sourceRoot` (additive field in `shared/apiTypes.ts`); a goal present in both roots is listed once with the workspace root as source. Verify: unit test in `goalStore`/route handler covering divergent roots, both-root overlap, and the single-root cases; `npm run typecheck` clean.
- [ ] 3.2 Client: the goals panel appends the source root as a quiet qualifier only when more than one root contributed. Verify: GoalPanel unit test with a single-root response (no qualifier rendered) and a two-root response (qualifier rendered, goal still actionable only per existing key rules).
- [ ] 3.3 Live evidence on 8505 at 393×850: seed a goal beside a session whose cwd is not the workspace root, open the goals panel, and screenshot the goal listed with its source qualifier - the owner's original screenshot scenario must now show the active goal, not "No goals recorded for this workspace."

## 4. Activity and notifications: unknown and failed are not empty

- [ ] 4.1 ChatView activity: `activity === undefined` renders a loading/unknown line, not "No subagent or background activity from this chat yet."; a failed first poll renders the failure. Verify: ChatView unit tests for the undefined slot and the failed-first-read path asserting neither empty string renders; `npx vitest run src/client/src/components/ChatView.subagents.test.ts` green.
  - [x] 4.1a The undefined leg: not-loaded renders "Activity has not been read for this chat yet."; the absence claim is spoken only by a delivered read that found nothing. (cecfcaae)
  - [ ] 4.1b The failed-first-read leg: no failure signal reaches ChatView today (no `activityFailed`-shaped property exists), so nothing renders the failure yet. Not started.
  - [x] 4.1c The unknown leg: a row whose status cannot be interpreted is neither active nor finished, so it survives the active scope's filter; the emptiness sentence is now spoken only about a list that is actually empty, so "Nothing running right now." can no longer print above visible rows. (cecfcaae)
- [x] 4.2 Session-scoped keys: on session switch within a workspace, the previous session's rows cannot render under the new one; retention continues while the key matches. Satisfied at the component layer rather than by clearing in the controller: rows are stamped with their session in `ChatView.willUpdate` and a stamp that does not match the selection reads as not-loaded, so a failed poll for B leaves B honest instead of showing A's rows. Covered by "does not render one session's rows under another session" in ChatView.subagents.test.ts; the controller-level unit test 4.2 names stays open as a stronger home for the failure leg. (cecfcaae)
- [ ] 4.3 Notifications: a failed read renders a failure line with the existing retry affordance; the two empty strings render only after a completed matching read. Verify: ChatView notification unit tests for failed/loaded/unloaded; `npx vitest run src/client/src/components/ChatView.notifications.test.ts` green.
- [ ] 4.4 Live evidence on 8505 at 393×850: (a) block the activity endpoint once (proxy or daemon pause) and screenshot the panel naming the failure; (b) switch between the two seeded sessions with the endpoint healthy and screenshot that neither session shows the other's rows; (c) normal path still shows "Nothing running right now." only when the read succeeded and nothing runs.

## 5. Integration and close-out

- [ ] 5.1 Full `npm run verify` (typecheck, lint, knip, tests) with exit 0, captured without piping; `npx tsc --noEmit` 0 errors. Verify: the command's own exit code, quoted in the task note.
- [ ] 5.2 On 8505, drive all four panels at 393×850 coarse pointer in one pass (goals loading/loaded/empty across the two seeded workspaces, activity with a live task, notifications failed and loaded) and record the numbers and screenshots; per the archive guidance, this change is archivable only after the browser pass. The daemon serves the goals and task reads, so if `src/server` files changed, restart the session daemon on 8505 before measuring.
- [ ] 5.3 e2e with Playwright MCP (real browser, 8505 stack, 393×850, coarse pointer) — the concrete acceptance pass for 5.2: (a) goals: switch from the seeded goal workspace to the no-goal workspace and assert the panel NEVER renders "No goals recorded for this workspace" while the fetch is in flight — sample the panel text at 100 ms during the switch and record every distinct string it shows; the owner's original screenshot state (disk has an active goal, panel claims none) must be re-checked against the live disk before the run and the run FAILS if the seed is absent. (b) activity: start a background task from the chat and assert the strip shows Running within one poll round-trip while "Nothing running right now." is absent, recording the latency; then compact the transcript (or use the seeded compacted session) and assert the task is STILL listed (registry attribution). (c) failure legs: block the goals/activity endpoints once via daemon pause and screenshot the failed-state line plus Retry; after Retry, screenshot the recovered list. (d) cross-session: switch between the two seeded sessions and screenshot that neither shows the other's runs. Every leg names its assertion and its number; a leg that cannot establish its precondition FAILS.
