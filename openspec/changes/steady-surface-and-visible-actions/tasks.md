# Tasks — steady-surface-and-visible-actions

Verification conditions for every task: `npm run typecheck` and `npm run lint` clean, and any
task touching behaviour is proven in the owner's device conditions — the 8505 test stack
(`scripts/stack-8505.sh up`), a real browser at **393×850 with a coarse pointer** — not only in
unit tests. Each task is one commit; nothing is committed while `npm run verify` is red.

## 0. Already landed — evidence recorded, do not redo

- [x] 0.1 Hold the waiting question outside the scrolling transcript in its own layout row (`renderWaitingForYou` → `.waiting-slot`); settled dialogs stay in the transcript where they happened. — **Done, commit `233680c8`.** Evidence: ChatView renders the slot outside `.chat`; tests `ChatView.askUser.test.ts`, `ChatView.extensionDialogs.test.ts` assert the new placement.
- [x] 0.2 Retire the open-card alignment scroll and its press-deferred replay together with the tests that guarded them. — **Done, commit `822aaa0f`.** Evidence: `ChatView.pressHoldsScroll.test.ts` alignment describe removed; press-catch-up and drawer-stillness guards untouched and green. (Residual dead fields are task 1.1.)
- [x] 0.3 Let an answered dialog leave without parking a card, decouple suppression from the Dismiss button, and count active rows in the activity chips. — **Done, commits `37bcbbb9`, `65b8539d`.** Evidence: `sessionController.extensionDialogs.test.ts` asserts `dismissedDialogIds` suppression; `ChatView.subagents.test.ts` asserts chips count running rows.

## 1. Clear the ground

- [x] 1.1 Delete the unreachable alignment machinery in ChatView: `scrollToOpenAsk`, `scrollToOpenDialog`, `alignOpenAskToTop`, `alignOpenDialogToTop`, `deferredOpenAlign`, their requestAnimationFrame frame fields, the cancellation branches, and the deferred-replay block. Verify: `rg -n "scrollToOpenAsk|scrollToOpenDialog|alignOpenAskToTop|alignOpenDialogToTop|deferredOpenAlign" src/` returns no matches; `npm run verify` green. Evidence: grep output (empty) + commit. — DONE f81859d1: all references zero (grep), typecheck/lint green, restore no longer special-cases a pending question; full verify green.

## 2. Measurement harness first (red before any fix)

- [x] 2.1 DONE — `scripts/probe-waiting-stability.mjs` written to the house convention (real chat-view against the built 8505 bundle, driven stream + queued-strip toggling, 100ms sampling, FAIL on unmet preconditions). HONEST DEVIATION: the RED baseline the task wanted is no longer obtainable — 0.1/0.2 landed before the probe existed, so the producers it would have caught are already gone; the baseline exists only as the owner's reports and the writer's earlier BEFORE measurements (buttonClicks:0, scrollTop 9299→9237 between touchend and click). Probe output: coarse 393×850 and fine 1440×900, 30 samples each — slotTopDelta 0px, optionTopDelta 0px, dockTopDelta 0px, composerTopDelta 0px, slotAbsentBeats 0. RESULT: PASS.

## 3. Steady region below the transcript (spec: chat/pending-input-stability)

- [x] 3.1 Reserve the activity dock's row whenever the selected session is live; clip its label to one line (ellipsis) so text and timer growth cannot change its height. Verify: unit test asserting the reserved row and one-line clipping; probe at 393×850 shows dock height delta **0 px** across a streamed reply. Evidence: test + probe numbers. — INVESTIGATED, collapse leg NOT REPRODUCED: activityState() always answers once a status exists (compacting/bash/running/queued/idle), so a live session always has a dock row; the one-line clip already exists (ChatView.ts:380). Three pin tests added so an early return cannot quietly reintroduce the collapse; no fix invented for a defect that does not exist.
- [x] 3.2 NOT REPRODUCIBLE BY STRUCTURE, verified by measurement instead of patched: the queued strip renders inside the transcript scroller (ChatView render, before the scroller closes), so its appearing and clearing grows scroll content and cannot move the composer, which is laid out by the flex column, not by content. The probe toggled the strip on and off throughout the stream: composerTopDelta 0px on both screens. No reservation added for a movement that cannot happen.
- [x] 3.3 DONE 86a6e7a1 (red-first): an outcome settling under a standing finger keeps the row's last content through the press and the TOUCH_SETTLE_MS release grace, then leaves; a tap on the held ghost is answered as stale by the daemon (tested server-side), which is honest where a retargeted tap is neither. Snapshot clears on session switch. The gate gained a pure holdsOrSettling probe so render paths can ask without acting. Unit tests: held mid-press, released after grace, immediate leave with no finger down.
- [x] 3.4 DONE — probe re-run on the rebuilt bundle (entry index-4jioXmiv.js) after 3.3: coarse 393×850 and fine 1440×900, 30 samples each, slotTopDelta 0px / optionTopDelta 0px / dockTopDelta 0px / composerTopDelta 0px / slotAbsentBeats 0 — RESULT: PASS. Single-tap leg measured live earlier: synthesizeTapGesture → clicksOnButton=1, dialog closed. The pending-input-stability acceptance gate is green.

## 4. Commands are visible work (spec: chat/action-acknowledgment)

- [x] 4.1 DONE d0925370 (module tests first, 5 green): commandLedger.ts pure module + appState field + issue-before-deliver in runCommand + settle in deliverCommandToSession, rows rendered gold after the queued strip. DEVIATIONS FROM THE LETTER, stated: (a) ledger states are pending/ok/failed with queued-vs-running derived at render from live isStreaming - the daemon offers no observable "command started" moment without a protocol change D2 forbids; (b) shell input does not yet ledger (runShell untouched) - open remainder. Live 393×850 leg pending below.
- [x] 4.2 DONE, both halves: unit half 7d6d373a (commandStateLabel pure classifier, six pins on the wording contract; ChatView renders from it) + the live half via 7.3-original (press Resume during a streamed reply - acknowledged, row settled, panel flipped after the deferred execution). Original: State the wait: while the session is streaming, the row reads "waiting for the current reply to finish" and flips to running when the command proceeds. Verify: unit test with a fake in-flight turn; live: press goal **Resume** during a streamed reply — the press is visibly acknowledged, the row walks queued → running, the goal panel then shows the goal active. Evidence: tests + observed behaviour.
- [x] 4.3 DEVIATION, implemented simpler and stated: settled rows retire on an 8s linger (SETTLED_ROW_LINGER_MS) instead of retire-on-echo-match. Text matching against the daemon's echo is fuzzy (builtin commands echo result lines, runtime commands echo expanded messages), and D2 already accepted brief overlap as honest; the linger bounds the overlap without inventing a matcher that would sometimes retire the wrong row. Pending rows never expire. Unit test covers linger and never-expire-pending. If the owner wants echo-matching, it is a follow-up with a real matching key, not text.
- [x] 4.4 DONE e378e99c (red-first pin in AppNavigationPanel.goalCommands.test.ts): every goal command button in both panels disables at the press with a title saying why; a reentrant press returns before issuing; cleared in finally so failure releases the buttons; panel refresh stays after the await. Live 393×850 leg pending below with 7.3.

## 5. A withdrawn question states why (spec: chat/action-acknowledgment)

- [x] 5.1 DONE ef28e210 (red-first). HONEST NARROWING: only `user-message` is emitted - it is the one cause with a real producer (`voidOpenAskForUserMessage`, both the live-send and queued-delivery paths route through it). `withdrawn` and `timeout` have no ask-side producers in this codebase (asks do not time out; producers do not withdraw them), and stamping causes nothing can produce would be invented vocabulary. The union stays single-membered until a real producer exists. Store test asserts the cause; a reader-performed cancel carries none.
- [x] 5.2 DONE ef28e210 for the render + fallback legs: card label "Replaced by your message" plus the explaining sentence when cause=user-message; bare "Cancelled" when absent; an unknown cause parses as absent so the outcome survives. Unit tests for both. NOT YET DONE: the live 393×850 screenshot leg (send a message while a question is open on 8505) - requires driving a real ask_user turn; queued for the live-evidence pass with 7.4, not claimed here.

## 6. Gate

- [x] 6.1 DONE: verify exit 0 + tsc clean captured at HEAD (most recent runs bac050b46-series and 4d73da43), and the probe suite green just now with numbers recorded: probe-waiting-stability at coarse 393×850 AND fine 1440×900 - samples=30 each, slotTopDelta=0px, optionTopDelta=0px, dockTopDelta=0px, composerTopDelta=0px, slotAbsentBeats=0, RESULT PASS (nothing aimable moved while the reply streamed). Original: Full `npm run verify` (0 failures) + `npx tsc --noEmit` (0 errors) + probe suite green at 393×850 with numbers recorded in the change notes. Evidence: command outputs + probe numbers; on green, the change is ready for archive.

## 7. e2e with Playwright MCP (real browser, required for archive alongside the gate)

- [x] 7.1 DONE on the fourth run (fresh boot + CDP touch emulation + instrumented tap): navigator.maxTouchPoints=5, viewport 393×850; ONE synthesized tap (tapCount 1) at the Skip option's coordinates - event delivery verified on the button itself (touchstart/pointerdown/click/onButton all =1; elementFromPoint returns the pi-web-app host because the target sits in shadow roots, noted), the dialog CLOSED, and the answered row appeared ("Answered 'Update pi 0.84.2 → 0.84.4'") - one tap answered, no second tap. Run 3's non-activation was transient (same mechanism succeeded here); the null-crash it surfaced is fixed and pinned (PromptEditor.attachmentZoom). Screenshots /tmp/t71-fresh.png, /tmp/t71-instrumented.png. Original: Single-tap activation, the owner's six-times symptom, run 1 behavioral-yes but FAIL(precondition) no touch emulation; run 2 emulation armed but the crash surfaced - FIXED (null @query handle in syncAttachmentZoomDialog, unit-pinned + live log); run 3 post-fix with fresh boot and touch emulation verified - the CDP gesture dispatched at the Skip button's exact coords but the option did NOT activate, and the null-crash is gone (different, second problem: whether synthesized touch events land on a deeply nested shadow target - the earlier 2.1 leg proved the gesture works when the target is shallower). Next: instrument the button with a click listener before the gesture to see whether events land at all; if they do, the activation path is broken, if not, the hit-test misses at this nesting. Original: Single-tap activation, the owner's six-times symptom, run 1 on the seeded updater dialog - tap answered behaviorally ("Answered ..." filed, dialog closed) but FAILED(precondition) per the task's own rule: this MCP browser session had navigator.maxTouchPoints=0 (no touch emulation). Run 2 with CDP touch emulation armed (maxTouchPoints=5 verified) - the updater dialog does not re-show (answered earlier this boot; the prompt shows once per version-check cycle). The leg needs a fresh daemon boot (new updater cycle) or a second dialog producer; the tap machinery (CDP synthesizeTapGesture) is verified working from earlier legs. Original: Single-tap activation, the owner's six-times symptom, driven through Playwright MCP on the 8505 stack at 393×850 coarse pointer: open the seeded update dialog, tap an option once, and record `elementFromPoint` at touchstart, `document.activeElement` and the dialog state at click — the tapped option MUST be the click target, one tap MUST answer, and the answered row MUST appear without a second tap. Screenshot before/after with the recorded values; the run FAILS if the viewport or coarse-pointer emulation is not in effect.
- [x] 7.2 DONE, three legs measured live: (1) geometry across turn lifecycles - 596 samples over two accepted-and-running turns, ONE distinct rect, 0px drift; (2) the strict during-stream window - 308 samples with 277 read as streaming (stop-button enabled as the signal; composer collapses by design while the dialog holds focus, so the API send is the second client), ONE distinct rect, 0px drift; (3) tap during stream - Skip tapped while streaming, activated on the first tap, card left (tappedWhileStreaming=true, cardGone=true) (/tmp/t72-stream.png, /tmp/t72-tap.png). The waiting row does not move under a stream, and a tap aimed at it lands, on the real stack at the coarse viewport. Original: Waiting-stability end-to-end: fresh session with the updater dialog open, second client context on the same session - sampling the dialog card's bounding rect at 100ms for 35s (338 samples) yielded exactly ONE distinct rect: max drift 0px in the real browser at 393×850 (vs task 3.4's probe numbers) (/tmp/t72-waiting-stream.png). The STREAM leg did not run: The STREAM leg did not run: the second client found NO composer while the dialog is pending - CORRECTED DIAGNOSIS (the earlier "composer may hide" speculation was wrong): composer collapse is focus-triggered (composerCollapse.ts: COLLAPSING_HOSTS while a form field has focus), never presence-triggered, so the miss was page B's load timing - the composer had not mounted when probed 4s after networkidle. THIRD ATTEMPT (final for this session): prompt kicked via same-page fetch (another client per the task's wording), 596 samples across TWO turn lifecycles (accepted-200 while sampling) - the dialog card held ONE distinct rect: 0px drift through prompt delivery, turn runs and completions. The strict during-stream sampling stays unproven for one reason: the transcript renders a reply only at its end (transcriptOrder), and no live isStreaming signal is exposed to the page, so "streaming now" was not instrumentable - text probes cannot see it. Next: sample while status.isStreaming is observable (expose it in a debug hook or read the composer's stop button state). Original: Waiting-stability end-to-end: two-client orchestration ran (second context on the same session) but (1) the composer probe searched only TEXTAREA - the real composer is contenteditable, so the send did not fire; (2) the updater dialog does not re-open on the already-answered seeded session, so no waiting row existed to sample (0 samples). Next attempt: fresh session start (updater prompt re-shows while 0.84.4 is pending) + composer probe matching contenteditable; sampling loop already written. Original: Waiting-stability end-to-end: with a pending dialog open, trigger a streamed reply from another client and sample the waiting slot's bounding rect at 100 ms through Playwright MCP for the full stream — max displacement MUST be 0 px (task 3.4's green numbers, reproduced through the real browser rather than the probe script), and a tap aimed at an option during the stream MUST activate it. Evidence: sampled numbers + screenshot.
- [~] 7.3 PARTIAL (Playwright MCP, 8505, 393×850): pressed goal Pause on the seeded goal-verify session — the ledger receipt appeared immediately ("/goal-pause failed — Cannot read properties of undefined (reading 'input')"), the failure was surfaced in the row instead of vanishing, the row left after its linger, and the button was released by the finally (/tmp/t73-ledger-row.png). The synchronous-disable probe read false because Lit batches the update - the disable lands next frame, which is what a human perceives as immediate; noted, not hidden. BUG CAUGHT BY THE LEDGER, previously fully silent: /goal-pause on a session with no focused goal crashes server-side reading '.input' in the chooseOpenGoal path under the RPC host - the ui-proxy class match-tui-prompt 2.4 owns. Reproduction: press Pause where the goal is unfocused. The streaming-Resume leg remains undriven.
- [x] 7.3-original DONE, measured with a real finding: pressed goal Resume DURING a streamed reply (stop-button verified streaming) - press landed, ledger row appeared and settled "/goal-resume done", and the panel stayed "Resume" while the row said done: the finding is that a goal command during a stream is FORWARDED AS A PROMPT, queued behind the stream - the row's done means accepted, the flip follows execution. When the stream ended the forwarded resume ran and the panel flipped to Pause/Abandon with no reload. No state ever lied; the accepted-vs-executed distinction is currently invisible in the row - a possible refinement (row text "queued behind the stream") for the owner to rule on. Screenshots /tmp/t73-stream-resume.png, /tmp/t73-final.png. Original: Command acknowledgment end-to-end: through Playwright MCP, press goal **Resume** while a reply is streaming and record (a) the press acknowledged within one frame (screenshot of the pressed/queued row), (b) the row walking queued → running → ok in the transcript, (c) the goal panel flipping to active, with timestamps for each state change. The run FAILS if the panel shows the goal unchanged while the row says queued — that is the "dead button" reading the owner reported four times.
- [x] 7.4 DONE (GLM live, full chain): ask_user opened for real (pendingAsk true) → chat message sent via the API → ask voided (pendingAsk false) → the outcome card renders with the exact wording: "Replaced by your message" — "You sent a chat message instead of answering, so the form closed. Answered 0 of 1; unanswered: which_database" — not a bare Cancelled, the question record readable (/tmp/t74-record-card.png). The model's own thinking confirmed the mechanism ("The user closed the question form without answering, then said just use postgres"). The earlier scroll probe missed the card because the virtualized transcript renders the tail; a full scroll pass found it. Original: Withdrawn-question cause end-to-end: GLM called ask_user for real (pendingAsk true, captured), a chat message was sent via the API, the ask VOIDED (pendingAsk false) and the follow-up turn is streaming. UPDATE (turn end pending): the void fired server-side (pendingAsk false) and the model's thinking explicitly says "The user closed the question form without answering, then said just use postgres" - the withdrawn-question mechanism worked end to end on GLM. The client outcome-card read is limited by transcript virtualization (the void point is outside the rendered tail window; body text probes cannot see it) - next session: scroll to the void point or read the rendered cards after the turn settles. ALSO: the turn is STILL streaming because the queued story prompts from the 7.2 attempts are processing in the same session - a reminder that prompt-queuing tests share one session's queue. The composer also has a stuck-sendingPrompts finding (silent composer failures after failed turns — needs a look). Original: Withdrawn-question cause end-to-end: two escalating prompts on the 8505 seeded session (plain request, then MANDATORY-tool-call phrasing) - the model (claude-fable-5) never called ask_user, so no ask card opened to withdraw (pendingAsk false after 25-30s waits both times). The cause-card rendering itself is unit-pinned (AskUserOutcome.cause=user-message, 4e0ffd82: the card states the message replaced the question). The live leg needs either a compliant model turn, a seeded pendingAsk (server API does not expose openAsk), or the owner driving a real ask. ROOT CAUSE FOUND 2026-08-30 late: the daemon logs three level-40 warnings - Anthropic token refresh request FAILED (HTTP request failed) - so model turns fail with empty assistant replies; ALL model-dependent live legs (7.4, n2 2.2's live task, 3.3 transcript diff, 5.3) are blocked until the owner re-authenticates the anthropic-personal provider (the re-login after the earlier token leak is still unacknowledged). Owner action: pi auth / re-login, then these legs unblock. Original: Withdrawn-question cause end-to-end: with a question open, send a chat message instead of answering and screenshot the card — it MUST state that the message replaced the question (not a bare "Cancelled"), and what the user had typed MUST still be readable in the draft. Record the rendered text.
