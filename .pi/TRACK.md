# 修复跟踪

流程:8505 验证(测试 + Playwright)→ GitHub 提交/tag → npm 发布 → nix 更新 8504。
**8504 全程不碰。**

规矩:先复现再改;每条有修复前会红的测试或浏览器实测;验证脚本前提不满足要 FAIL 不空过;tsc/lint 才算真检验;verify 跑动时不改仓库。

---

## 已完成(pi-web,已提交未发布)

- [x] 返回手势失灵 —— 路由比较漏 `view`;实测:修复前连按 4 次不动,修复后 back#1 即返回
- [x] 重连后被换到别的会话 —— 刷新工作区列表时当前会话不在其中就改选;3 条测试
- [x] 筛选项目显示"无会话" —— 工作区未加载,空集合当成"没有"
- [x] 后台运行显灰 —— 解析器没点名 `backgroundRunCount`
- [x] 刷新丢草稿 —— 只在切会话时读回;6 条测试含"先擦后读"防护
- [x] 机器/工作区不能搜索 —— 8 条测试
- [x] 对话框正文压住选项 —— 限高未声明溢出行为
- [x] 终端两条滚动条 —— xterm 自绘 + 我们又强制常显
- [x] 语音功能从未可用 —— 客户端解析器从未点名 `speechToText`

## 已完成(pi-goal,需 sessiond 重载才生效)

- [x] `/goal-resume` 不管用 —— `3c7345b`
- [x] `set_goal_tasks` 崩溃 —— `91c3aab`(`ctx.ui.custom` 在 pi-web 下返回 undefined)
- [x] paused 仍每 4 秒注入 —— `421a66a`

> 三条都在 sessiond 启动(08-25 10:22)之后提交,当前进程未加载。发布后随 nix 更新一并生效。

---

> 截图未落盘(`.pi-web/attachments` 为空),用手机状态栏时间做唯一标识。

## 待办

- [ ] **`role: custom` 的消息没有身份** —— 真实会话 200 条里有 2 条(后台任务通知),字段只有 `content/customType/details/role`,**没有 timestamp**
  - 后果:身份为 undefined,不参与去重;若这类消息被投递两次会重复显示
  - 未观察到实际重复,不照推测改。真要修应由服务端为它们盖时间戳
  - 实测命令见 `.pi/HANDOFF.md`;当时结果:200 条消息、**零身份冲突**、2 条无身份

- [ ] **按钮重合**(用户 9:05 截图,反复报了三次)
  - 我在 320/360/393 三档量过:控件 5 个、零溢出、**两两无碰撞**。所以我复现不了
  - **缩放也排除了**:`node scripts/verify-composer-row-fits.mjs 8505 1.5` 在 320/360/393 三档仍零重叠,且脚本会校验 zoom 真的生效(未生效即 FAIL)
  - 已测维度:屏宽三档 × 流式状态 × 可见性/透明度 × 两两相交 × 缩放 1.5 —— 全部复现不了
  - **需要的现场信息**:①手机浏览器里 `window.innerWidth` 的值;②设置里缩放滑条的当前档位;③重合的是**哪两个**按钮
  - 取法:手机浏览器地址栏输入 `javascript:alert(innerWidth+" / "+localStorage.getItem("pi-web-app-scale"))`(部分浏览器禁用,则在桌面同尺寸窗口复现)
- [ ] **底部内容被屏幕吞掉**(同截图)
  - 现象:卡片下半截在可视区外,滚不到
  - 与上一条可能同源(缩放导致布局超出),也可能是 `100vh` 在移动端浏览器地址栏下的经典问题 → 查是否该用 `100dvh`


- [ ] **宽内容冲出消息卡片 + 粘性头压住表格首行**(截图 9:05)
  - 现象:表格右侧被切在**屏幕边缘**而非卡片圆角处;`ASSISTANT` 粘性头盖住表格第一行
  - 已排除:`.table-scroll` 有 `max-width: 100%; overflow-x: auto`,表格自身会滚动 —— 病灶在它外层
  - 下一步:量 `.msg` 的实际宽度与 `.chat` 内容盒对比;若 `.msg` 更宽,查 `--pi-chat-measure`(现为 100%)在有宽子元素时是否被撑开(`min-width: 0` 缺失是常见成因)
  - 粘性头那条:`.msg-header` z-index 4 覆在内容上是设计,但表格首行被盖住说明**内容需要 `scroll-margin-top` 或头部需要不透明背景**

- [x] **返回要按两下**(我上一轮修完路由后残留)—— 实测:打开一个工具留下 **2 条历史**,第一下返回只是撤销重复帧,看着像没反应
  - 两个成因:①三处独立写 URL(`route.ts`/`namespacedQueryArgs.ts`/`settingsRoute.ts`),一次动作分几笔写入各压一条 → 400ms 内合并为一条;②打开工具面板会压"占位帧",用选择关闭时又压一条 → 后续路由写入**覆盖**该帧
  - 实测:`->files` 由 hist+2 变 hist+1;三下返回走三步(原来三下只走两步)
  - 变异检验:去掉占位帧消费立刻 FAIL
- [x] **桌面路由护栏失效**(我引入的回归)—— `routeMatchesUrl` 假定缺 view ⇒ navigation,而桌面 `defaultRouteView` 是 chat,比较永远不等 → 默认值改为**由调用方传入**,三条测试

- [x] **界面抖动**(出错/重试/确认时)
  - 用户描述:"一直有新的信息挤进来,界面一直抖动,根本没法看/操作,快速加载闪烁抖来抖去"
  - **已复现**:390px 下驱动错误横幅反复出现/消失 → **21 次位移,累计 1.019**(0.1 才读作平稳)
  - 修复:横幅出现即显示,但**撤下要等它被看清**(`bannerHold.ts`,1500ms)→ **21 次降到 5 次**
  - 变异检验:把停留改成 0,脚本立刻 PASS→FAIL(5→21)
  - **两次试过让横幅浮起来不占位,都不成立**:贴顶会盖住上下文条(那是"你在哪个会话"的唯一出口);用 `--pi-chat-sticky-top` 偏移则该变量是转录内部粘头用的、值为 -26px;且上下文条 z-index 20 > 横幅 8,浮起来反而被压在后面看不见
  - 390px 空闲状态实测 CLS=0,**未复现**。需在有错误横幅/重试/确认的状态下再测。
  - 脚本:`scripts/verify-no-layout-thrash.mjs`(拒绝空过:引擎不报 layout-shift 即 FAIL)
- [x] **一条消息显示两遍**
  - 截图 2:53 / 2:38:同一条文本两个气泡,一个 `Queued to steer`、一个 `Read`
  - 我推测的两个成因**已被证伪**(实测:append 带 clientMessageId 能对上;刷新时队列只有自己)
  - 新线索:`attachQueuedPromptClientIds` 按**文本**配对 → 同文本两条会张冠李戴。触发条件:排队时连发两条相同文本。
  - **第三个成因也证伪**:实测排两条完全相同的文本,各自拿到自己的 id(`followUp:Then say DONE.:cm-record-b` / `:cm-record-c`),`scripts/verify-duplicate-queued-bubble.mjs`
  - 另一条未验:`piSessionService.ts:3927/3934` 压缩队列排空丢 `clientMessageId`
  - **第四次找到并证实了**:`transcriptWithPendingInQueueOrder` 只登记状态**恰为 `queued`** 的气泡。消息离开浏览器到下一帧状态之间停在 `sending`,配不上 → 另合成一行 → 同一条画两遍
  - 三条红测试:①带 id 但状态是 sending 不再重画;②队列条目无 id 时按文本+种类认领气泡;③两条相同文本仍是两条(按"认领"消费,不会合并)
  - 变异检验:把条件改回 `state !== "queued"` 立刻红
- [x] **问题卡片:选 Custom 后输入框被底栏压住** —— sticky 底栏会被前面的内容滚过;用 `scroll-margin-bottom` 让出余量(不增高卡片,354px 仍装得下 360px 视口)
  - 截图(桌面):`Custom answer` 文本域与 sticky 底栏(Back / Next)重叠,输入框只露出上下两条边
- [x] **问题卡片:Back 与 Next 尺寸悬殊** —— 两者同档 `flex: 0 1 auto` + 各自最小宽
  - Back 约 70px,Next 撑满剩余宽度;主次分层过头,读作两个不同层级的控件
- [x] **投递状态三种混用**(黄条 queued / 打勾 Read / 无标记)
  - 截图 2:49,用户原话:"一会又msg queue(黄色的那个)一会又是read打勾,然后有的消息又没打勾,到底用哪种方式显示?很乱"
  - 根因:历史消息无投递记录 → 无标记;本次会话的消息到 `delivered` 后**永久挂着 ✓✓** → 同一条已完成的消息刷新前后长得不一样
  - 规则:**标记只报未定的结果**;被接收后安静,与历史消息一致
  - tsc 抓到我按截图字面把状态名写成 `read`,真实状态是 `delivered`
- [~] **长对话卡顿** —— 待现场复现
  - 用户原话:"现在页面聊天多了很卡,加载优化下吧"(当时该会话 10657 条消息)
  - 实测:转录是**分页加载**的,不是全量渲染(预览会话只渲染 8 行 / 67 个 DOM 节点)。手头没有长会话数据,量不出卡顿
  - 测量工具已就位:`scripts/verify-long-transcript-cost.mjs`(渲染 0 行即 FAIL)
  - 需要你那条 10657 条消息的会话才能定位
- [~] **回底按钮与状态条重叠** —— **构造上不可能**
  - 截图 2:46:圆钮与跨屏宽的 "receiving response" 状态条重合
  - 回底按钮在 `.chat-wrap` 内(`position: relative`),状态条是它的**兄弟节点且在流内**,两者不共享定位上下文;实测 overlaps=[]
  - 若你仍能看到重叠,需要当时的屏宽与状态条文案
- [x] **抽屉展开后不能切 ACTIVITY/GOALS/NOTIFICATIONS**
  - 组件层构造出三个分区后证实:展开后分区**仍在、也能切**(2 条测试)
  - 真因是窄屏:`.drawer-tab` 为 `flex: 0 0 auto` + `nowrap`,**绝不收缩**,选中项滚入视野就把其余顶出屏幕 —— 读起来就是"标签条不见了"
  - 改为可收缩 + 标签省略号;另加"原路收回"测试
  - 截图 2:44:展开后顶部只剩范围码(All 205 / Subagents 1 / Agent runs 14 / Tasks 190),标签条不见了
- [x] **层级失控** —— 原本 10001/10000/100/50/40/30/26/25/20/15/10 各写各的,**`AuthDialog` 只有 10,被 QuickSwitcher(25)盖住**:让你重新登录的框开在你正在读的面板后面
  - 改为一条尺度(`index.html` 的 `:root`),按"打断到什么程度"分层:raised 10 / sticky 20 / popover 30 / overlay 40 / dialog 50 / blocking 60
  - 16 个组件迁移;三条测试守住:尺度已发布、顺序单调、**没有组件再自己挑数字**(>9 即红)
- [x] **通知盖住模态层** —— 层级尺度统一后顺带解决:活动坞在 sticky(20)、快速切换在 overlay(40);加测试钉死这条关系
  - 截图 2:45:后台任务完成通知("Verify route and scope fixes / VERIFY_EXIT=0")盖在快速切换面板的会话列表上
- [x] **跨工作区选会话不同步祖先**
  - 截图 2:13:在 be-dev 会话的对话里,切到 Sessions 视图显示的是 pi-web 工作区的列表
  - `selectSession` 只写 `selectedSession`;现由会话的 cwd 反查工作区、再由工作区反查项目(`sessionAncestors.ts`)
  - 目录未编目时**不动选择**(未编目 ≠ 当前选择是错的)
  - 4 条纯函数测试 + 2 条控制器测试;变异检验:去掉接线立刻红
- [x] **非对话视图无常驻返回**
  - 截图 2:15:GOALS 面板打开后关不掉,"必须一直切直到他没了才回到 chat"
  - 截图 12:35:抽屉展开后同样没有出路
  - 复现:`scripts/verify-way-back-to-chat.mjs` 在 Files 视图下找不到任何回对话的控件
  - 修复:**面包屑去它所指之处** —— 不在对话里时会话面包屑回对话,在对话里时才列出别的会话;无障碍名同步说明去向
  - 脚本先假通过过一次(把 `background` 里的 "back" 当成出口),已收紧为整词 + 只看无障碍名 + **按下去验证真的落在对话**
  - 变异检验:改回"永远列会话"立刻 FAIL
- [~] **761–1180px 死胡同** —— **证伪**。实测 700/900/1100/1200px 每档都有出口(`scripts/verify-navigation-reach.mjs`)。那条 `max-width:1180px` 隐藏的 `header` 不是应用外壳的那个,报告的推断没落到实处
- [~] **跨机器加载 / 消除 N+1** —— 推迟 —— 服务端函数就绪(实测 34ms vs 395ms,11.6×),未接线;接口加到 gateway 会牵动 6 文件 40 处测试替身

- [x] **活动汇总把人为动作报成故障** —— 截图显示 "46 failed · 166 done"
  - `killed`(你主动停的)与 `lost`(跟丢了)都被并入 **failed**;`lost` 的标签又写作 "Stopped",把"我们跟丢"说成"你停了"
  - 现在四种各归各:Running / Done / **Stopped**(你停的)/ **Lost**(跟丢的)/ Failed
  - 另修:`stopped`/`lost` 原本落进 "done" 堆;`stopped` 未被认作终态,会永远算作未完成的活
  - 5 条新测试;两条既有测试的期望按新词表订正(旧表让 killed 与 lost 同名为 Stopped)

## 已量过、未发现缺陷(不改)

- 竖直空间:对话占屏高 **65%**(469/727),上下文条 53 + 抽屉 53 + 输入区 119 + 状态条 32(`scripts/verify-vertical-budget.mjs`)
- 输入区控件行:381px 宽、**零溢出**,五个控件全可见;320/360/393 三档发送键均在
- 回底按钮与状态条:构造上不可能重叠(兄弟节点、不共享定位上下文)

## 待用户验收

- [x] 终端单滚动条 —— 已有测试钉住(`TerminalPanel.test.ts` 断言不得为 `overflow-y: scroll`);真机观感仍待你确认
- [x] 缩放滑条 —— `uiScale.test.ts` 4 条 + 设置面板测试;80–150% 的手感仍待你确认
- [x] 抽屉返回路径 —— 测试证实由同一个控件原路收回

---

## 设计报告(已完成,供收敛用)

- 状态模型:"谁跟随谁"的规则**散在每个调用点手写**,无集中定义
- 页面流转:"我在哪"有 **8 个记录处**,各有独立写入者;3 个 blocker
- 边界契约:静默丢弃 1 处用户可见 + 6 处潜在;推荐 `AllFields<T>` + `satisfies`(编辑器即报错,CI 无需新门禁);**不选契约测试**(字段清单与类型同漂移)

> `parsers.test.ts` 曾把"丢弃"固化为预期 —— 测试在保护 bug,补字段反而变红。
